diff --git a/FEXCore/Source/Interface/Context/Context.cpp b/FEXCore/Source/Interface/Context/Context.cpp
index de8415b..6190889 100644
--- a/FEXCore/Source/Interface/Context/Context.cpp
+++ b/FEXCore/Source/Interface/Context/Context.cpp
@@ -14,6 +14,8 @@
 #include <FEXCore/Core/Thunks.h>
 #include "FEXCore/Debug/InternalThreadState.h"
 
+#include <cstdio>
+
 namespace FEXCore::Context {
 fextl::unique_ptr<FEXCore::Context::Context> FEXCore::Context::Context::CreateNewContext(const FEXCore::HostFeatures& Features) {
   return fextl::make_unique<FEXCore::Context::ContextImpl>(Features);
@@ -55,4 +57,56 @@ FEXCore::CPUID::FunctionResults FEXCore::Context::ContextImpl::RunCPUIDFunctionN
 bool FEXCore::Context::ContextImpl::IsAddressInCodeBuffer(FEXCore::Core::InternalThreadState* Thread, uintptr_t Address) const {
   return Thread->CPUBackend->IsAddressInCodeBuffer(Address) || CodeCache.IsAddressInMappedCodeBuffer(Address);
 }
+
+bool FEXCore::Context::ContextImpl::DumpDispatcherStateForDiagnostics(FEXCore::Core::InternalThreadState* Thread, char* OutBuf,
+                                                                       size_t OutBufSize) const {
+  if (Thread == nullptr || Thread->CurrentFrame == nullptr || OutBuf == nullptr || OutBufSize == 0 || !Dispatcher) {
+    return false;
+  }
+  const auto Known = Dispatcher->GetDiagnosticAddresses();
+  const auto& Live = Thread->CurrentFrame->Pointers;
+
+  // Field-by-field: dispatcher's known-good (freshly re-derived, always correct by
+  // construction) value vs. whatever this thread's live Pointers struct actually holds for
+  // the same conceptual address. A mismatch means InitThreadPointers() either never ran for
+  // this field, ran before the dispatcher's own value was finalized, or something later wrote
+  // over it -- as opposed to a dispatcher-level translation bug, which would show the *same*
+  // (wrong) value in both columns.
+  // ReuseGenerationCounterAddress and ExitFunctionLinkValidatedGeneration were never included
+  // here before -- added specifically to test whether the iOS reuse-in-place generation-check
+  // fallback (Dispatcher.cpp's ExitFunctionLinker, JIT.cpp's ExitFunctionLink) is actually wired
+  // up for whichever thread crashes next. If ReuseGenerationCounterAddress reads 0 here, that
+  // check is silently skipped entirely for this thread (cbz falls straight through to trusting
+  // TMP1 unconditionally) -- a completely different bug from the race the mechanism itself is
+  // designed to close, but one that would produce the exact same symptom (stale HostCode branch
+  // target, "not tracked" JIT allocation fault).
+  const uint64_t LiveGenValue = Live.ReuseGenerationCounterAddress != 0
+    ? *reinterpret_cast<const volatile uint64_t*>(Live.ReuseGenerationCounterAddress)
+    : 0;
+  const int Written = std::snprintf(
+    OutBuf, OutBufSize,
+    "dispatcher known-good: DispatchPtr=%#llx Start=%#llx End=%#llx ExitFunctionLinker=%#llx "
+    "DispatcherLoopTop=%#llx | "
+    "thread live Pointers: DispatcherLoopTop=%#llx%s ExitFunctionLinker=%#llx%s "
+    "DispatcherLoopTopFillSRA=%#llx DispatcherLoopTopEnterEC=%#llx DispatcherLoopTopEnterECFillSRA=%#llx "
+    "SignalReturnHandler=%#llx SignalReturnHandlerRT=%#llx ThreadStopHandlerSpillSRA=%#llx "
+    "ThreadPauseHandlerSpillSRA=%#llx | ReuseGenerationCounterAddress=%#llx%s live_gen=%llu "
+    "ExitFunctionLinkValidatedGeneration=%llu",
+    static_cast<unsigned long long>(Known.DispatchPtr), static_cast<unsigned long long>(Known.Start),
+    static_cast<unsigned long long>(Known.End), static_cast<unsigned long long>(Known.ExitFunctionLinkerAddress),
+    static_cast<unsigned long long>(Known.DispatcherLoopTopAddress),
+    static_cast<unsigned long long>(Live.DispatcherLoopTop),
+    (Live.DispatcherLoopTop == Known.DispatcherLoopTopAddress ? "(matches)" : "(MISMATCH)"),
+    static_cast<unsigned long long>(Live.ExitFunctionLinker),
+    (Live.ExitFunctionLinker == Known.ExitFunctionLinkerAddress ? "(matches)" : "(MISMATCH)"),
+    static_cast<unsigned long long>(Live.DispatcherLoopTopFillSRA), static_cast<unsigned long long>(Live.DispatcherLoopTopEnterEC),
+    static_cast<unsigned long long>(Live.DispatcherLoopTopEnterECFillSRA), static_cast<unsigned long long>(Live.SignalReturnHandler),
+    static_cast<unsigned long long>(Live.SignalReturnHandlerRT), static_cast<unsigned long long>(Live.ThreadStopHandlerSpillSRA),
+    static_cast<unsigned long long>(Live.ThreadPauseHandlerSpillSRA),
+    static_cast<unsigned long long>(Live.ReuseGenerationCounterAddress),
+    (Live.ReuseGenerationCounterAddress == 0 ? " NOT-WIRED-CHECK-SKIPPED" : ""),
+    static_cast<unsigned long long>(LiveGenValue),
+    static_cast<unsigned long long>(Thread->CurrentFrame->ExitFunctionLinkValidatedGeneration));
+  return Written > 0;
+}
 } // namespace FEXCore::Context
diff --git a/FEXCore/Source/Interface/Context/Context.h b/FEXCore/Source/Interface/Context/Context.h
index e374d85..0764d29 100644
--- a/FEXCore/Source/Interface/Context/Context.h
+++ b/FEXCore/Source/Interface/Context/Context.h
@@ -21,6 +21,7 @@
 #include <atomic>
 #include <cstddef>
 #include <cstdint>
+#include <functional>
 #include <mutex>
 #include <optional>
 #include <shared_mutex>
@@ -223,9 +224,102 @@ public:
     return CodeInvalidationMutex;
   }
 
+  // ClearCodeCache's NewCodeBuffer=false path (physically reusing an already-granted JIT
+  // buffer in place, an iOS-only workaround for StikDebug's external BreakpointJIT mechanism
+  // failing on a session's 3rd+ fresh-allocation request) clears the calling thread's own L1/L2
+  // lookup caches and the shared L3 cache/block-links, but has no visibility into any *other*
+  // guest thread's independent L1/L2 cache -- those are owned per-thread by application-level
+  // code above this file (AetherPS4::Fex::GuestEngine, which tracks every live
+  // InternalThreadState), not by anything FEXCore's own core keeps a list of. A stale L1/L2 hit
+  // on another thread bypasses CodeInvalidationMutex and CompileBlock entirely (fast-path
+  // lookups only ever consult L1/L2, per this file's "backends only check L1 and L2, not L3"
+  // invariant) and branches straight into now-reused, differently-contented memory -- confirmed
+  // on-device as a distinct fault signature from every other issue fixed today ("not tracked --
+  // not inside any currently-live JIT allocation" -- i.e. genuinely wrong content at a
+  // still-valid-looking address, not an alias mixup). Set by GuestEngine at setup so
+  // ClearCodeCache can invoke it (passing the calling thread so its already-cleared cache isn't
+  // redundantly touched) instead of leaving other threads' caches to only self-correct on their
+  // own next CompileBlock call, which may never come if a thread is mostly just executing
+  // already-cached code.
+  //
+  // The lk parameter is the SAME write-lock token ClearCodeCache already holds (from
+  // Thread->LookupCache->AcquireWriteLock() just above the call site) -- pass it straight
+  // through rather than having the callback acquire anything new. On iOS every guest thread's
+  // LookupCache::Shared points at the one process-wide L3 cache tied to the single, StikDebug-
+  // count-limited JIT buffer (see JIT.cpp's ThreadState->LookupCache->Shared assignment), so
+  // "another thread's write lock" and "the lock we're already holding" are literally the same
+  // mutex, not independent per-thread locks. An earlier version of this callback tried to
+  // (re-)acquire that same lock per other-thread, which can only ever self-conflict: blocking
+  // acquisition deadlocked outright, and a try-lock fallback failed 100% of the time, every
+  // thread, every call, with zero real contention involved -- confirmed on-device.
+  std::function<void(FEXCore::Core::InternalThreadState* CallingThread, const FEXCore::LookupCacheWriteLockToken& lk)>
+    OnBufferReusedInPlace;
+
+  // Safepoint hooks around JIT buffer invalidation. Confirmed on-device: OnBufferReusedInPlace
+  // above (and the delinking GuestToHostMap::ClearCache already does) closes the "another
+  // thread's stale L1/L2 entry" and "another thread's stale direct branch" gaps, but neither
+  // stops a thread that's already MID-EXECUTION of an already-resolved direct branch, right as
+  // the memory it jumps into gets cleared/reused out from under it -- "a thread running
+  // already-JIT'd code never touches CodeInvalidationMutex at all", so holding it exclusively
+  // during a reuse does not, on its own, block a concurrent executor the way it blocks a
+  // concurrent compiler. Seen on-device as two different guest threads both faulting into the
+  // same stale dispatcher region within microseconds of each other, right as a third thread's
+  // buffer-reuse was in flight, once Rocket League reached genuine multi-threaded rendering.
+  //
+  // BeginBufferInvalidationSafepoint should pause every other live guest thread (via an async
+  // signal to each, unrelated to anything guest-visible) and only return once they're
+  // confirmed paused (or a bounded wait times out) -- called right before ANY code memory a
+  // stale branch could be executing through gets touched. EndBufferInvalidationSafepoint
+  // releases them, called once that memory is safe to run through again. Set by GuestEngine at
+  // setup, same as OnBufferReusedInPlace, since it's the one place a full thread list (with
+  // native OS thread handles) exists at all.
+  //
+  // "Safe to run through again" is stricter than it first looks: a thread paused mid-block
+  // (not at a link site) keeps executing forward through whatever bytes are physically at its
+  // resume PC once released, and those bytes are still the *original* ones right up until
+  // something actually overwrites them -- ClearCodeCache's NewCodeBuffer=false path (Core.cpp)
+  // only clears metadata (LookupCache/BlockLinks), it never itself touches the buffer's bytes.
+  // The caller that goes on to physically repopulate that buffer (JIT.cpp's CompileCode, via its
+  // post-clear memcpy) is therefore the one that must hold this pause open -- calling Begin
+  // before ClearCodeCache(..., false) and End only after that memcpy finishes, not just around
+  // the clear itself. Ending it too early was confirmed on-device to still let a just-resumed
+  // thread race that memcpy into the exact bytes it was already executing.
+  //
+  // Ordering relative to CodeInvalidationMutex matters too, and it's the opposite of what seems
+  // natural: the caller must acquire that mutex exclusively *before* calling Begin, not after.
+  // BeginBufferInvalidationSafepoint's pause signal can land on another guest thread at any point
+  // in its execution, including mid-way through CompileBlock or ExitFunctionLink while that thread
+  // holds this same mutex *shared* -- a signal handler can't run that thread's own unlock, so it
+  // stays frozen holding the shared reference for as long as the pause lasts. If the pausing
+  // thread then tries to acquire the mutex exclusively *after* pausing everyone, it can deadlock
+  // against exactly the thread it just froze mid-hold: confirmed on-device as a
+  // "BACHATA_BUFFER_REUSE: begin" with no matching completion ever following it, the session
+  // stuck indefinitely with zero further progress despite the safepoint's own accounting showing
+  // every other thread successfully paused. Acquiring the mutex first avoids this by construction
+  // -- a successful exclusive acquire already proves (via ordinary mutex semantics) that no thread
+  // currently holds it shared, so pausing everyone afterward can never catch one mid-hold of it.
+  std::function<void(FEXCore::Core::InternalThreadState* CallingThread)> BeginBufferInvalidationSafepoint;
+  std::function<void(FEXCore::Core::InternalThreadState* CallingThread)> EndBufferInvalidationSafepoint;
+
+  // Owned by GuestEngine (fex_guest_engine.cpp), set at the same setup point as the two members
+  // just above. Points at a process-wide std::atomic<uint64_t> incremented once per completed
+  // reuse-in-place cycle -- see JITPointers::ReuseGenerationCounterAddress and
+  // CpuStateFrame::ExitFunctionLinkValidatedGeneration for what this closes: the safepoint
+  // mechanism above protects every thread it successfully pauses *before* a reuse starts, but
+  // signal delivery has real, nonzero latency, and a thread whose own trip through
+  // ExitFunctionLinker's post-call window (Dispatcher.cpp) happens to finish faster than that
+  // latency can execute a stale branch before ever being signaled at all -- confirmed on-device
+  // as the actual remaining source of the reuse-in-place crash even with that redirect already
+  // in place. This generation counter is the fallback for exactly that gap: a live, lock-free
+  // check ExitFunctionLinker performs itself, right before trusting the value ExitFunctionLink
+  // (JIT.cpp) handed back, rather than relying solely on having been paused in time.
+  std::atomic<uint64_t>* ReuseGenerationCounter = nullptr;
+
   void ConfigureAOTGen(FEXCore::Core::InternalThreadState* Thread, fextl::set<uint64_t>* ExternalBranches, uint64_t SectionMaxAddress) override;
 
   bool IsAddressInCodeBuffer(FEXCore::Core::InternalThreadState* Thread, uintptr_t Address) const override;
+  bool DumpDispatcherStateForDiagnostics(FEXCore::Core::InternalThreadState* Thread, char* OutBuf,
+                                         size_t OutBufSize) const override;
 
   // returns false if a handler was already registered
   std::optional<CustomIRResult>
diff --git a/FEXCore/Source/Interface/Core/ArchHelpers/Arm64Emitter.cpp b/FEXCore/Source/Interface/Core/ArchHelpers/Arm64Emitter.cpp
index 8856b44..73fb0e9 100644
--- a/FEXCore/Source/Interface/Core/ArchHelpers/Arm64Emitter.cpp
+++ b/FEXCore/Source/Interface/Core/ArchHelpers/Arm64Emitter.cpp
@@ -73,11 +73,33 @@ namespace x64 {
     ARMEmitter::Reg::r8, ARMEmitter::Reg::r16, ARMEmitter::Reg::r17,
   };
 
+#if defined(__APPLE__)
+  // On Apple platforms x18 is reserved by the OS as the "platform register": the kernel,
+  // signal delivery, and other system runtime code are documented as free to clobber it at any
+  // time, independent of anything the running code itself does. Upstream FEX includes it in this
+  // pool unconditionally (fine on Linux/Android, which impose no such reservation), so a value
+  // FEX's own register allocator assigns to x18 here can read back silently corrupted moments
+  // after being computed. Confirmed on-device via a 100%-reproducible Rocket League crash: a
+  // compiled RIP-relative-address computation (mov/movk/movk) into x18, immediately followed by
+  // a store through it (stlur), where the disassembled code at the fault is unambiguously
+  // correct -- x18 loaded with a valid non-null guest address right before the store -- yet the
+  // store faults writing to address 0. The block that triggers it executes this exact
+  // compute-into-x18-then-store pattern hundreds of times in a row, which is consistent with a
+  // rare, timing-dependent OS clobber rather than a compile-time miscalculation (the same
+  // pattern was byte-for-byte identical between compile time and crash time, ruling out a JIT
+  // buffer/memory bug). Excluding x18 from the allocatable pool entirely removes the exposure.
+  constexpr std::array<ARMEmitter::Register, 6> RA = {
+    // All these callee saved
+    ARMEmitter::Reg::r20, ARMEmitter::Reg::r21, ARMEmitter::Reg::r22, ARMEmitter::Reg::r23,
+    ARMEmitter::Reg::r24, ARMEmitter::Reg::r30,
+  };
+#else
   constexpr std::array<ARMEmitter::Register, 7> RA = {
     // All these callee saved
     ARMEmitter::Reg::r20, ARMEmitter::Reg::r21, ARMEmitter::Reg::r22, ARMEmitter::Reg::r23,
     ARMEmitter::Reg::r24, ARMEmitter::Reg::r30, ARMEmitter::Reg::r18,
   };
+#endif
 
   constexpr unsigned RAPairs = 4;
 
diff --git a/FEXCore/Source/Interface/Core/Core.cpp b/FEXCore/Source/Interface/Core/Core.cpp
index b9aa740..dba3057 100644
--- a/FEXCore/Source/Interface/Core/Core.cpp
+++ b/FEXCore/Source/Interface/Core/Core.cpp
@@ -110,11 +110,22 @@ struct GetFrameBlockInfoResult {
   const CPU::CPUBackend::JITCodeTail* InlineTail;
 };
 static GetFrameBlockInfoResult GetFrameBlockInfo(FEXCore::Core::CpuStateFrame* Frame) {
-  const uint64_t BlockBegin = Frame->State.InlineJITBlockHeader;
+  // InlineJITBlockHeader is a PC-relative (adr-computed) address written by the JIT's own
+  // generated entry-point code (Arm64JITCore::EmitEntryPoint: `adr TMP1, HeaderLabel`), so it
+  // always points at the EXECUTABLE-side alias of the code buffer. On iOS's dual-mapped JIT
+  // memory that alias is not guaranteed readable -- Utils/ArchHelpers/Arm64.cpp's own SIGBUS
+  // backpatch handler hit this exact trap already (see its detailed comment at the equivalent
+  // dereference): reading the executable alias directly, from inside a signal handler, produced
+  // a second, undeliverable fault that silently killed the handling thread with no crash report
+  // and no further log output. Translate to the writable alias first, same as that fixed site;
+  // the header/tail layout and offsets are address-independent (same physical page), so this is
+  // a no-op on every platform except iOS, where GetWritableAddress does a real translation.
+  const uint64_t BlockBegin = reinterpret_cast<uint64_t>(
+      FEXCore::Allocator::GetWritableAddress(reinterpret_cast<void*>(Frame->State.InlineJITBlockHeader)));
   auto InlineHeader = reinterpret_cast<const CPU::CPUBackend::JITCodeHeader*>(BlockBegin);
 
   if (InlineHeader) {
-    auto InlineTail = reinterpret_cast<const CPU::CPUBackend::JITCodeTail*>(Frame->State.InlineJITBlockHeader + InlineHeader->OffsetToBlockTail);
+    auto InlineTail = reinterpret_cast<const CPU::CPUBackend::JITCodeTail*>(BlockBegin + InlineHeader->OffsetToBlockTail);
     return {InlineHeader, InlineTail};
   }
 
@@ -138,6 +149,12 @@ uint64_t ContextImpl::GetGuestBlockEntry(FEXCore::Core::InternalThreadState* Thr
 
 uint64_t ContextImpl::RestoreRIPFromHostPC(FEXCore::Core::InternalThreadState* Thread, uint64_t HostPC) {
   const auto Frame = Thread->CurrentFrame;
+  // BlockBegin stays in the same (executable-alias) address space as HostPC -- both come from
+  // live PC values (InlineJITBlockHeader is adr-computed by the JIT's own generated code, see
+  // GetFrameBlockInfo's comment), so the range check and StartingHostPC accumulation below only
+  // ever compare/add numeric PC values, never dereference through BlockBegin directly. Only the
+  // RIPEntry table itself needs the writable-alias translation, since that's an actual memory
+  // read -- GetFrameBlockInfo already applies it internally for InlineHeader/InlineTail.
   const uint64_t BlockBegin = Frame->State.InlineJITBlockHeader;
   auto [InlineHeader, InlineTail] = GetFrameBlockInfo(Thread->CurrentFrame);
 
@@ -147,8 +164,12 @@ uint64_t ContextImpl::RestoreRIPFromHostPC(FEXCore::Core::InternalThreadState* T
     // This is currently as close as FEX can get RIP reconstructions.
     if (HostPC >= reinterpret_cast<uint64_t>(BlockBegin) && HostPC < reinterpret_cast<uint64_t>(BlockBegin + InlineTail->Size)) {
 
-      auto RIPEntry =
-        reinterpret_cast<const uint8_t*>(Frame->State.InlineJITBlockHeader + InlineHeader->OffsetToBlockTail + InlineTail->OffsetToRIPEntries);
+      // InlineHeader/InlineTail already point into the writable alias (translated inside
+      // GetFrameBlockInfo) -- derive RIPEntry from InlineTail directly instead of re-deriving
+      // from the raw, execute-only Frame->State.InlineJITBlockHeader the way this line
+      // originally did, which would dereference unreadable iOS JIT memory (see
+      // GetFrameBlockInfo's comment for the exact on-device failure mode this caused).
+      auto RIPEntry = reinterpret_cast<const uint8_t*>(InlineTail) + InlineTail->OffsetToRIPEntries;
 
       // Reconstruct RIP from JIT entries for this block.
       uint64_t StartingHostPC = BlockBegin;
@@ -496,9 +517,63 @@ void ContextImpl::ClearCodeCache(FEXCore::Core::InternalThreadState* Thread, boo
     // Allocate new CodeBuffer + L3 LookupCache and clear L1+L2 caches
     Thread->CPUBackend->ClearCache();
   } else {
-    // Clear L1+L2 cache of this thread, and clear L3 cache across any threads using it
+    // Clear L1+L2 cache of this thread, and clear L3 cache across any threads using it.
+    //
+    // NewCodeBuffer=false physically reuses the same already-allocated buffer memory instead
+    // of moving to a fresh allocation -- unlike the NewCodeBuffer=true path above, where other
+    // threads' already-compiled code and in-flight execution are simply left alone in the old,
+    // still-valid buffer. Reusing memory that another thread could be *executing* out of right
+    // now (not just compiling into -- a thread running already-JIT'd code never touches
+    // CodeInvalidationMutex at all) needs the same exclusion this codebase's existing code-
+    // invalidation entry points (InvalidateCodeBuffersCodeRange, InvalidateThreadCachedCodeRange)
+    // already rely on CodeInvalidationMutex for.
+    //
+    // Unlike those entry points, though, this function does NOT acquire that lock itself -- the
+    // caller (Arm64JITCore::CompileCode, on the iOS-only path that reaches this branch) must
+    // already hold it exclusive, AND must already have BeginBufferInvalidationSafepoint active,
+    // before calling in here. This used to be the other way around (acquire the mutex here, then
+    // pause other threads), which deadlocks: BeginBufferInvalidationSafepoint's pause signal can
+    // land on another guest thread at literally any point in its execution, including while that
+    // thread is mid-way through CompileBlock or ExitFunctionLink holding this same mutex *shared*
+    // (via GuardSignalDeferringSection) -- a signal handler can't run that thread's own unlock, so
+    // it stays frozen holding the shared reference until its pause is lifted, which can't happen
+    // until this function's caller finishes and calls EndBufferInvalidationSafepoint, which can't
+    // happen until this function returns, which can't happen until the exclusive lock is granted --
+    // a real, permanent deadlock, confirmed on-device as a "BACHATA_BUFFER_REUSE: begin" with no
+    // matching "got CodeInvalidationMutex exclusive" ever following it, session stuck indefinitely
+    // (CPU still busy from other threads, but zero further progress) despite the safepoint's own
+    // accounting showing all other threads successfully paused. Acquiring the mutex *before*
+    // signaling the pause avoids this by construction: a successful exclusive acquire already
+    // proves no thread currently holds it shared (ordinary mutex semantics guarantee that), so
+    // freezing every other thread afterward can never catch one mid-hold of it.
+    //
+    // The pause itself (BeginBufferInvalidationSafepoint/EndBufferInvalidationSafepoint) also has
+    // to span not just this metadata clear but the caller's subsequent memcpy that overwrites the
+    // buffer's bytes with newly compiled code -- a thread paused mid-block (not at a link site)
+    // and then resumed keeps executing forward through whatever bytes are physically there, which
+    // are still the *original*, unchanged bytes immediately after this clear, since nothing has
+    // overwritten them yet at this point. It's only unsafe once those bytes actually change, which
+    // doesn't happen until that later memcpy. Ending the safepoint before that memcpy was
+    // separately confirmed on-device to let a just-resumed thread race a concurrent compile's
+    // memcpy into the exact bytes it was already executing -- two different guest threads faulting
+    // on instruction-fetch at addresses only 0x80 apart, both landing in memory the LookupCache
+    // had already forgotten about, is exactly the signature of that race. See CompileCode's own
+    // comment (JIT.cpp) for the full Begin/lock -> clear -> memcpy -> End/unlock ordering.
+    LogMan::Msg::IFmt("BACHATA_BUFFER_REUSE: begin, Thread={:#x}", reinterpret_cast<uintptr_t>(Thread));
     auto lk = Thread->LookupCache->AcquireWriteLock();
+    LogMan::Msg::IFmt("BACHATA_BUFFER_REUSE: got own LookupCache write lock, clearing");
     Thread->LookupCache->ClearCache(lk);
+    LogMan::Msg::IFmt("BACHATA_BUFFER_REUSE: own cache cleared, invoking OnBufferReusedInPlace={}",
+                      OnBufferReusedInPlace ? "set" : "unset");
+    // Still inside the caller's exclusive lock here, so any other thread that might attempt
+    // to compile (and so update its own L1/L2 from this now-current L3 state) stays blocked
+    // until the caller releases it (after its own subsequent memcpy) -- see OnBufferReusedInPlace's
+    // own comment (Context.h) for why other threads' independent L1/L2 caches need to be reached
+    // at all.
+    if (OnBufferReusedInPlace) {
+      OnBufferReusedInPlace(Thread, lk);
+    }
+    LogMan::Msg::IFmt("BACHATA_BUFFER_REUSE: metadata clear done, returning to caller");
   }
   Allocator::VirtualDontNeed(Thread->CallRetStackBase, FEXCore::Core::InternalThreadState::CALLRET_STACK_SIZE);
 }
@@ -830,6 +905,24 @@ ContextImpl::CompileCodeResult ContextImpl::CompileCode(FEXCore::Core::InternalT
   };
 }
 
+} // namespace FEXCore::Context
+
+// Forward declaration only, deliberately not an #include of Bachata-S4's own app-level header
+// from this vendored FEXCore file -- this is temporary, diagnostic-only wiring (see its call
+// site below, inside ContextImpl::CompileBlock), not a permanent architectural dependency. Both
+// this file and its real definition (fex_guest_engine.cpp) end up in the same final static
+// library, so this links correctly as long as the signature matches exactly. Declared here, at
+// true global scope with FEXCore::Context closed and reopened around it, rather than nested
+// three levels deep inside it (this whole file is one un-closed `namespace FEXCore::Context {`
+// block starting near its top) -- a nested `namespace AetherPS4::Fex {}` here would silently
+// declare FEXCore::Context::AetherPS4::Fex::... instead of the real, top-level one, which then
+// fails to link against the call site's `::AetherPS4::Fex::...`.
+namespace AetherPS4::Fex {
+void BachataRecordKnownBlockSnapshot(uintptr_t writable_code_ptr, const unsigned char* bytes, std::size_t len) noexcept;
+}
+
+namespace FEXCore::Context {
+
 uintptr_t ContextImpl::CompileBlock(FEXCore::Core::CpuStateFrame* Frame, uint64_t GuestRIP, uint64_t MaxInst) {
   if constexpr (BLOCK_DEBUGGING) {
     // Block debugging logic is hand-written and needs to be handled with care.
@@ -862,6 +955,70 @@ uintptr_t ContextImpl::CompileBlock(FEXCore::Core::CpuStateFrame* Frame, uint64_
 
   auto [CompiledCode, DebugData, StartAddr, Length, NeedsAddGuestCodeRanges] = CompileCode(Thread, GuestRIP, MaxInst);
   auto CodePtr = CompiledCode.EntryPoints[GuestRIP];
+
+  // Diagnostic only, gated to a single known-deterministic guest RIP: a 100%-reproducible
+  // "write to address 0x0" crash traces back to this exact block (a VEX MOVAPS pair with
+  // RIP-relative operands), with the fault's own register dump showing a scratch register (x18)
+  // as zero even though the host code physically at the fault PC, read moments after the fault,
+  // disassembles as a correct mov/movk/movk sequence that should have set it. That could mean
+  // the disassembly-after-the-fact is stale (something rewrote this exact memory between fault
+  // and read), or it could mean the compile-time-emitted bytes were never actually correct at
+  // this address in the first place and the crash dump's read is accurate. Logging the freshly
+  // compiled host entry address and its first bytes right here, at the moment of compilation
+  // (before this thread or any other could possibly have executed or reused any of it), settles
+  // which one it is.
+  if (GuestRIP == 0x7001a19ed0ULL && CodePtr != nullptr) {
+    auto* Bytes = reinterpret_cast<const unsigned char*>(
+      FEXCore::Allocator::GetWritableAddress(const_cast<void*>(reinterpret_cast<const void*>(CodePtr))));
+    // Capture the *entire* compiled fragment, not just its first few bytes -- the crash this is
+    // chasing faults partway through a block (not necessarily at its very entry point), so a
+    // short, fixed-size snapshot from CodePtr could miss the actual faulting address entirely
+    // if the block is longer than that. CompiledCode.Size is the true host code size for this
+    // exact compile; capped defensively since this is a fixed-size diagnostic buffer, not
+    // because any real block is expected to approach that cap.
+    const std::size_t CaptureLen = std::min<std::size_t>(CompiledCode.Size, 4096);
+    fextl::string HexDump;
+    for (std::size_t i = 0; i < std::min<std::size_t>(CaptureLen, 64); ++i) {
+      HexDump += fmt::format("{:02x} ", Bytes[i]);
+    }
+    LogMan::Msg::EFmt("BACHATA_COMPILE_SNAPSHOT: GuestRIP={:#x} CodePtr={:#x} CompiledSize={:#x} first64bytes={}",
+                      GuestRIP, reinterpret_cast<uintptr_t>(CodePtr), CompiledCode.Size, HexDump);
+    // Also dump the raw *guest* x86 bytes this block was actually compiled from -- FEX
+    // identity-maps guest memory into the host process, so GuestRIP is directly readable as a
+    // host pointer, no translation needed. This sidesteps offline PS4 SELF-format extraction
+    // entirely (individually compressed segments made a naive raw-file dump of the exported
+    // eboot.bin unreliable past the first instruction or two -- disassembly repeated
+    // suspiciously, a classic sign of reading through non-code/compressed bytes) by capturing
+    // exactly what the JIT itself decoded, straight from the live, already-decompressed guest
+    // image.
+    //
+    // A prior fixed 2048-byte capture only covered the first 248 x86 instructions -- a
+    // branch-free run of RIP-relative vmovaps/mov stores -- and never reached the actual fault
+    // site, which the ARM64-side offset math places roughly 65% of the way through this
+    // (unusually large, 24852-byte host) block. `Length` here is CompileCode's own guest-side
+    // byte length for this exact block (destructured above from its CompileCode() call), so
+    // using it instead of a guess guarantees this single capture covers the real fault site
+    // regardless of how large the block turns out to be.
+    {
+      const auto* GuestBytes = reinterpret_cast<const unsigned char*>(GuestRIP);
+      fextl::string GuestHexDump;
+      const std::size_t GuestCaptureLen = std::min<std::size_t>(std::max<std::size_t>(Length, 2048), 65536);
+      for (std::size_t i = 0; i < GuestCaptureLen; ++i) {
+        GuestHexDump += fmt::format("{:02x}", GuestBytes[i]);
+      }
+      LogMan::Msg::EFmt("BACHATA_GUEST_SOURCE_SNAPSHOT: GuestRIP={:#x} BlockLength={:#x} len={:#x} bytes={}", GuestRIP,
+                        Length, GuestCaptureLen, GuestHexDump);
+    }
+    // Also stash the full capture for a byte-for-byte crash-time comparison (see
+    // AetherPS4::Fex::BachataCompareKnownBlockSnapshot, called from signals.cpp) -- a plain
+    // forward declaration rather than including Bachata-S4's own app-level header from this
+    // vendored FEXCore file, since this is temporary, diagnostic-only wiring, not a permanent
+    // architectural dependency. Same final static library, so this links correctly as long as
+    // the signature here matches the real definition exactly (namespace included -- name
+    // mangling depends on it).
+    ::AetherPS4::Fex::BachataRecordKnownBlockSnapshot(reinterpret_cast<uintptr_t>(Bytes), Bytes, CaptureLen);
+  }
+
   if (CodePtr == nullptr) {
     return 0;
   } else if (!DebugData) {
@@ -968,6 +1125,14 @@ uintptr_t ContextImpl::CompileSingleStep(FEXCore::Core::CpuStateFrame* Frame, ui
 void ContextImpl::InvalidateCodeBuffersCodeRange(uint64_t Start, uint64_t Length) {
   FEXCORE_PROFILE_SCOPED("InvalidateCodeBuffersCodeRange");
 
+  // Diagnostic only, temporary: unconditional (not gated behind a debug log level) since real
+  // invalidations should be rare -- typically self-modifying-code detection or an explicit
+  // guest unmap/remap, not a hot path -- so this is cheap insurance while chasing a
+  // reproducible crash whose root cause (compiled-correct-but-wrong-at-crash-time memory, or
+  // something else) is still unconfirmed. Directly answers whether anything ever invalidates a
+  // range overlapping the one known-deterministic guest RIP under investigation.
+  LogMan::Msg::EFmt("BACHATA_INVALIDATE_CODE_RANGE: Start={:#x} Length={:#x} End={:#x}", Start, Length, Start + Length);
+
   LOGMAN_THROW_A_FMT(CodeInvalidationMutex.try_lock() == false, "CodeInvalidationMutex needs to be unique_locked here");
   std::scoped_lock lk {CodeBufferListLock};
   auto it = CodeBufferList.begin();
@@ -982,6 +1147,11 @@ void ContextImpl::InvalidateCodeBuffersCodeRange(uint64_t Start, uint64_t Length
 }
 
 void ContextImpl::InvalidateThreadCachedCodeRange(FEXCore::Core::InternalThreadState* Thread, uint64_t Start, uint64_t Length) {
+  // Diagnostic only, temporary: see InvalidateCodeBuffersCodeRange's own comment, just above --
+  // same reasoning, same investigation.
+  LogMan::Msg::EFmt("BACHATA_INVALIDATE_THREAD_RANGE: Thread={:#x} Start={:#x} Length={:#x} End={:#x}",
+                    reinterpret_cast<uintptr_t>(Thread), Start, Length, Start + Length);
+
   LOGMAN_THROW_A_FMT(CodeInvalidationMutex.try_lock() == false, "CodeInvalidationMutex needs to be unique_locked here");
 
   // Ensures now-modified mappings aren't cached as being in their previous non-executable state.
diff --git a/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.cpp b/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.cpp
index 19719c2..c422199 100644
--- a/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.cpp
+++ b/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.cpp
@@ -64,7 +64,15 @@ void Dispatcher::EmitDispatcher() {
   const auto DisasmBegin = GetCursorAddress<const vixl::aarch64::Instruction*>();
 #endif
 
-  DispatchPtr = GetCursorAddress<AsmDispatch>();
+  // DispatchPtr is a raw C function pointer other code CALLS directly (see fex_guest_engine.cpp
+  // on iOS) -- unlike relative branches encoded entirely within this same buffer, an escaping
+  // absolute pointer like this one must be the executable (RX) address, not the write (RW)
+  // address GetCursorAddress() returns. Identity on every platform except iOS.
+  // DispatchRawBegin keeps the untranslated (RW) cursor too -- the dispatcher's bytes below are
+  // all written through it, so the end-of-emission ClearICache call needs both aliases (see the
+  // comment there for why one alone isn't enough on iOS's dual-mapped scheme).
+  void* const DispatchRawBegin = GetCursorAddress<void*>();
+  DispatchPtr = reinterpret_cast<AsmDispatch>(FEXCore::Allocator::GetExecutableAddress(DispatchRawBegin));
 
   // while (true) {
   //    Ptr = FindBlock(RIP)
@@ -212,6 +220,28 @@ void Dispatcher::EmitDispatcher() {
       // Shift the offset by the size of the block cache entry
       add(TMP1, TMP1, TMP2, ARMEmitter::ShiftType::LSL, FEXCore::ilog2(sizeof(LookupCache::LookupCacheEntry)));
 
+      // Snapshot the reuse-generation counter before reading the cached HostCode below, for the
+      // same reason and using the same mechanism as ExitFunctionLinker's own check (JITPointers::
+      // ReuseGenerationCounterAddress, see its comment, CoreState.h). This lookup is lock-free
+      // exactly like that one, but unlike it never calls into C++ at all -- there's no
+      // ExitFunctionLink-equivalent to piggyback a validated value on, so both the "before" and
+      // "after" reads have to happen entirely within this routine. TMP3 is unused elsewhere in
+      // this routine, so it's safe as a snapshot register across everything below.
+      //
+      // BlockLookupGenerationUnwired guards ONLY the dereference on the next line (can't safely
+      // dereference a null ReuseGenerationCounterAddress) -- it must never skip past the actual
+      // lookup logic below. An earlier version of this check used one label for both "unwired,
+      // skip the whole mechanism" and "skip just my own re-check at the end", which for the
+      // *unwired* case jumped straight past the entire original lookup (the ldp/sub/cbnz/cbz/L1
+      // update sequence below), leaving TMP4 completely uninitialized before the final `br` --
+      // confirmed on-device as a new, unrelated-looking crash (garbage register values, fault
+      // address 0x0) that had nothing to do with JIT reuse at all, introduced by this fix itself.
+      ARMEmitter::ForwardLabel BlockLookupGenerationUnwired;
+      ldr(TMP3, STATE_PTR(CpuStateFrame, Pointers.ReuseGenerationCounterAddress));
+      (void)cbz(ARMEmitter::Size::i64Bit, TMP3, &BlockLookupGenerationUnwired);
+      ldr(TMP3, TMP3, 0);
+      (void)Bind(&BlockLookupGenerationUnwired);
+
       // The the full LookupCacheEntry with a single LDP.
       // Check the guest address first to ensure it maps to the address we are currently at.
       // This fixes aliasing problems
@@ -236,12 +266,46 @@ void Dispatcher::EmitDispatcher() {
 
         stp<ARMEmitter::IndexType::OFFSET>(TMP4, RipReg, TMP1);
 
+        // Re-check right before trusting TMP4: if the generation changed since the snapshot
+        // above, a reuse-in-place cycle completed while this thread was mid-lookup, and TMP4 may
+        // no longer point at valid code -- SafepointSignalHandler's own signal-based redirect for
+        // this exact range (fex_guest_engine.cpp) can miss this if this thread's own trip through
+        // the lookup finishes faster than signal delivery latency, same as ExitFunctionLinker's
+        // equivalent gap. Redirect to AbsoluteLoopTopAddress (this routine's own start) instead of
+        // trusting TMP4 -- safe unconditionally, since nothing above this point has any guest- or
+        // host-visible side effect (the L1 cache write just above is host-only bookkeeping,
+        // harmlessly stale/overwritten by whatever this retry finds).
+        //
+        // Re-loads and re-checks ReuseGenerationCounterAddress itself here rather than reusing
+        // TMP3's zero-ness, deliberately: TMP3 legitimately reads as 0 both when unwired (nothing
+        // to compare against) AND when wired but no reuse cycle has happened yet this session (a
+        // real, valid snapshot that just happens to be 0) -- conflating those would skip this
+        // whole check for that second, perfectly normal case.
+        ARMEmitter::ForwardLabel SkipBlockLookupRecheck;
+        ldr(TMP1, STATE_PTR(CpuStateFrame, Pointers.ReuseGenerationCounterAddress));
+        (void)cbz(ARMEmitter::Size::i64Bit, TMP1, &SkipBlockLookupRecheck);
+        ldr(TMP1, TMP1, 0);
+        sub(TMP1, TMP1, TMP3);
+        (void)cbz(ARMEmitter::Size::i64Bit, TMP1, &SkipBlockLookupRecheck);
+        ldr(TMP4, STATE_PTR(CpuStateFrame, Pointers.DispatcherLoopTop));
+
+        (void)Bind(&SkipBlockLookupRecheck);
         // Jump to the block
         br(TMP4);
       }
     }
   }
 
+  // This whole block-cache lookup routine (from AbsoluteLoopTopAddress, captured above, through
+  // here) is deliberately lock-free -- it's the hottest path in the entire JIT, executed on
+  // every guest dispatch. It reads a cached guest->host mapping and, on a hit, holds the cached
+  // host address in a register (TMP4 above) for the short window between that read and the
+  // `br(TMP4)` that consumes it. See SafepointSignalHandler's own comment
+  // (fex_guest_engine.cpp) for why a thread paused by this codebase's iOS JIT-buffer-reuse
+  // safepoint mechanism during that window is unsafe, and why it redirects such a thread back to
+  // AbsoluteLoopTopAddress using exactly this end boundary.
+  DispatcherBlockLookupRangeEndAddress = GetCursorAddress<uint64_t>();
+
   {
     ThreadStopHandlerAddressSpillSRA = GetCursorAddress<uint64_t>();
     SpillStaticRegs(TMP1);
@@ -309,7 +373,52 @@ void Dispatcher::EmitDispatcher() {
       FillStaticRegs();
     });
 
+    // TMP1 may be a branch target inside code that was just compiled (or re-linked) by
+    // the call above, on iOS's dual-mapped RW/RX JIT scheme. Cache maintenance
+    // (ClearICache -> sys_icache_invalidate, see JIT.cpp/Allocator.cpp) should already
+    // include its own barriers, but this is the exact "compile/patch, then immediately
+    // branch to it" sequence that's been the hardest bug to fully close in this port, so
+    // an explicit ISB here is cheap, iOS-only-in-effect insurance against any remaining
+    // instruction-fetch-vs-cache-maintenance ordering gap on the executing core.
+    isb();
+
+    // SafepointSignalHandler (fex_guest_engine.cpp) redirects a thread caught anywhere in this
+    // routine IF it gets signaled in time -- but signal delivery has real, nonzero latency, and
+    // a thread whose own trip through this window finishes faster than that latency executes
+    // the branch below having never been signaled at all. This is the fallback: a live,
+    // lock-free check performed right here, comparing the reuse-generation counter's current
+    // value against what ExitFunctionLink (JIT.cpp) recorded as current when it validated the
+    // HostCode now in TMP1. Confirmed on-device as the actual remaining source of the
+    // reuse-in-place crash even with the signal-based redirect alone covering this range.
+    //
+    // ReuseGenerationCounterAddress is plain data (not a JIT code address, see its own comment,
+    // CoreState.h) -- 0 if never wired up (this reuse-in-place mechanism is iOS-only), in which
+    // case this check is skipped entirely and TMP1 is trusted exactly as before this fix, with
+    // no other change in behavior.
+    ldr(TMP2, STATE_PTR(CpuStateFrame, Pointers.ReuseGenerationCounterAddress));
+    ARMEmitter::ForwardLabel ProceedToExitFunctionLinkerBranch;
+    (void)cbz(ARMEmitter::Size::i64Bit, TMP2, &ProceedToExitFunctionLinkerBranch);
+    ldr(TMP2, TMP2, 0); // Dereference: TMP2 = current live generation.
+    ldr(TMP3, STATE_PTR(CpuStateFrame, ExitFunctionLinkValidatedGeneration));
+    sub(TMP2, TMP2, TMP3);
+    (void)cbz(ARMEmitter::Size::i64Bit, TMP2, &ProceedToExitFunctionLinkerBranch);
+
+    // Mismatch: a reuse completed since ExitFunctionLink validated this HostCode. Redirect to
+    // DispatcherLoopTop instead of trusting it -- safe unconditionally, since ExitFunctionLink
+    // already set State.rip to the real target on every return path specifically for this case.
+    ldr(TMP1, STATE_PTR(CpuStateFrame, Pointers.DispatcherLoopTop));
+
+    (void)Bind(&ProceedToExitFunctionLinkerBranch);
     br(TMP1);
+
+    // See JITPointers::DispatcherBlockLookupRangeEnd's own comment (CoreState.h) and
+    // ExitFunctionLink's final-return comment (JIT.cpp) for the full story: this whole
+    // ExitFunctionLinkerAddress routine, from its start above through this point, is a second
+    // window with the exact same class of hazard as the block-cache lookup below -- it takes
+    // ExitFunctionLink's C++ return value in TMP1 and branches straight to it (the `br` just
+    // above) with no lock or synchronization of its own. A thread safepoint-paused anywhere in
+    // this range may already be holding that value with the branch still pending.
+    ExitFunctionLinkerRangeEndAddress = GetCursorAddress<uint64_t>();
   }
 
   // Need to create the block
@@ -343,7 +452,10 @@ void Dispatcher::EmitDispatcher() {
       FillStaticRegs();
     });
 
-    // Jump to the compiled block
+    // Jump to the compiled block. TMP1 is a branch target inside code CompileBlock just
+    // finished writing (see the ExitFunctionLink path above's comment on why an explicit
+    // ISB is cheap insurance here specifically, on iOS's dual-mapped JIT scheme).
+    isb();
     br(TMP1);
   }
 
@@ -376,7 +488,9 @@ void Dispatcher::EmitDispatcher() {
       FillStaticRegs();
     });
 
-    // Jump to the compiled block
+    // Jump to the compiled block. Same just-compiled-then-immediately-branched-to
+    // ordering concern as the NoBlock/ExitFunctionLink paths above.
+    isb();
     br(TMP1);
   }
 
@@ -477,7 +591,11 @@ void Dispatcher::EmitDispatcher() {
     // On return to the thunk, the thunk can get whatever its return value is from the thread context depending on ABI handling on its end
     // When the thunk itself returns, it'll do its regular return logic there
     // void ReentrantCallback(FEXCore::Core::InternalThreadState *Thread, uint64_t RIP);
-    CallbackPtr = GetCursorAddress<JITCallback>();
+    // CallbackPtr is invoked as a raw function pointer from ContextImpl::HandleCallback for
+    // thunk-reentrancy callbacks (GuestEngine::CallGuest), the same "escapes as a direct call
+    // target" pattern as DispatchPtr above, so it needs the same RW->RX translation.
+    CallbackPtr =
+        reinterpret_cast<JITCallback>(FEXCore::Allocator::GetExecutableAddress(GetCursorAddress<void*>()));
 
     // We expect the thunk to have previously pushed the registers it was using
     PushCalleeSavedRegisters();
@@ -605,7 +723,18 @@ void Dispatcher::EmitDispatcher() {
   dc64(PMFCompileSingleStep.GetConvertedPointer());
 
   Start = reinterpret_cast<uint64_t>(DispatchPtr);
-  End = GetCursorAddress<uint64_t>();
+  void* const DispatchRawEnd = GetCursorAddress<void*>();
+  // End is compared against a faulting thread's PC by the signal delegator, and on iOS a fault
+  // always lands on the executable (RX) side, so this boundary must be RX too, matching Start
+  // (already RX via DispatchPtr).
+  End = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(DispatchRawEnd));
+
+  // Both aliases need clearing here, same reasoning as JIT.cpp's post-compile ClearICache: the
+  // dispatcher's bytes were written through the RW cursor (DispatchRawBegin/DispatchRawEnd), so
+  // that's the alias whose dirtied data-cache line actually needs flushing, while DispatchPtr/End
+  // (RX) is the alias later fetched from and needs any stale icache entries invalidated there.
+  ClearICache(DispatchRawBegin,
+              reinterpret_cast<uintptr_t>(DispatchRawEnd) - reinterpret_cast<uintptr_t>(DispatchRawBegin));
   ClearICache(reinterpret_cast<void*>(DispatchPtr), End - reinterpret_cast<uint64_t>(DispatchPtr));
 
   if (CTX->Config.BlockJITNaming()) {
@@ -2611,7 +2740,24 @@ uint64_t Dispatcher::GenerateABICall(FallbackABI ABI) {
   // Return to JIT
   ret();
 
-  return Address;
+  // Address is the write-side (RW) address GetCursorAddress() returned at the top of this
+  // function, same as every other named handler address in this file (F64SinHandlerAddress,
+  // ExitFunctionLinkerAddress, etc.) -- but unlike those, which all get translated via
+  // GetExecutableAddress() before being stored where JIT'd code can branch to them
+  // (InitThreadPointers's Ptrs.* assignments), this function's caller
+  // (Dispatcher::EmitDispatcher's `ABIPointers[ABI] = GenerateABICall(ABI);` loop) stores the
+  // return value directly into ABIPointers with no translation anywhere downstream:
+  // InterpreterOps::FillFallbackIndexPointers copies it verbatim into
+  // Pointers.FallbackHandlerPointers[...].ABIHandler, which JIT'd guest code then loads and
+  // calls directly (`ldr TMP1, STATE_PTR(..., ABIHandler); blr TMP1;`, see the F64FPREM1
+  // fallback just above as one example) for every F80/F64 transcendental-math fallback
+  // operation. Confirmed on-device: this is the actual source of a fault that looked like
+  // stale-JIT-buffer corruption but recurred identically after a full app relaunch with a
+  // freshly allocated dispatcher buffer -- a hot floating-point fallback path (plausible for
+  // any game doing significant world-gen/physics math) branching to this untranslated write
+  // address over and over, millions of times, each fixed for one instruction by
+  // TryRecoverJitAliasFault's signal-time recovery but never for the actual cached target.
+  return reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(Address)));
 }
 
 void Dispatcher::InitThreadPointers(FEXCore::Core::InternalThreadState* Thread) {
@@ -2619,36 +2765,90 @@ void Dispatcher::InitThreadPointers(FEXCore::Core::InternalThreadState* Thread)
   {
     auto& Ptrs = Thread->CurrentFrame->Pointers;
 
-    Ptrs.DispatcherLoopTop = AbsoluteLoopTopAddress;
-    Ptrs.DispatcherLoopTopFillSRA = AbsoluteLoopTopAddressFillSRA;
-    Ptrs.DispatcherLoopTopEnterEC = AbsoluteLoopTopAddressEnterEC;
-    Ptrs.DispatcherLoopTopEnterECFillSRA = AbsoluteLoopTopAddressEnterECFillSRA;
-    Ptrs.ExitFunctionLinker = ExitFunctionLinkerAddress;
-    Ptrs.ThreadStopHandlerSpillSRA = ThreadStopHandlerAddressSpillSRA;
-    Ptrs.ThreadPauseHandlerSpillSRA = ThreadPauseHandlerAddressSpillSRA;
-    Ptrs.GuestSignal_SIGILL = GuestSignal_SIGILL;
-    Ptrs.GuestSignal_SIGTRAP = GuestSignal_SIGTRAP;
-    Ptrs.GuestSignal_SIGSEGV = GuestSignal_SIGSEGV;
-    Ptrs.SignalReturnHandler = SignalHandlerReturnAddress;
-    Ptrs.SignalReturnHandlerRT = SignalHandlerReturnAddressRT;
-    Ptrs.LUDIVHandler = LUDIVHandlerAddress;
-    Ptrs.LDIVHandler = LDIVHandlerAddress;
-    Ptrs.F64SinHandler = F64SinHandlerAddress;
-    Ptrs.F64CosHandler = F64CosHandlerAddress;
-    Ptrs.F64TanHandler = F64TanHandlerAddress;
-    Ptrs.F64F2XM1Handler = F64F2XM1HandlerAddress;
-    Ptrs.F64ScaleHandler = F64ScaleHandlerAddress;
-    Ptrs.F64AtanHandler = F64AtanHandlerAddress;
-    Ptrs.F64FYL2XHandler = F64FYL2XHandlerAddress;
-    Ptrs.F64FYL2XP1Handler = F64FYL2XP1HandlerAddress;
-    Ptrs.F64FPREMHandler = F64FPREMHandlerAddress;
-    Ptrs.F64FPREM1Handler = F64FPREM1HandlerAddress;
+    // All of these are branch/return targets reached directly from JIT-generated ARM64 code
+    // (dispatcher loop top, signal return, thread pause/stop, and the div/transcendental-math
+    // fallback ABI thunks). GetCursorAddress() above returned the write-side (RW) address the
+    // emitter used while generating this code; on iOS that's a different virtual address than
+    // the one that's actually executable, so every address that escapes here as something later
+    // branched to must be translated through GetExecutableAddress() first (identity elsewhere).
+    Ptrs.DispatcherLoopTop = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(AbsoluteLoopTopAddress)));
+    Ptrs.DispatcherBlockLookupRangeEnd = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(DispatcherBlockLookupRangeEndAddress)));
+    Ptrs.DispatcherLoopTopFillSRA = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(AbsoluteLoopTopAddressFillSRA)));
+    Ptrs.DispatcherLoopTopEnterEC = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(AbsoluteLoopTopAddressEnterEC)));
+    Ptrs.DispatcherLoopTopEnterECFillSRA =
+        reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+            reinterpret_cast<void*>(AbsoluteLoopTopAddressEnterECFillSRA)));
+    Ptrs.ExitFunctionLinker = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(ExitFunctionLinkerAddress)));
+    Ptrs.ExitFunctionLinkerRangeEnd = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(ExitFunctionLinkerRangeEndAddress)));
+    // Plain data address, not JIT code -- deliberately NOT translated through
+    // GetExecutableAddress() (see ReuseGenerationCounterAddress's own comment, CoreState.h).
+    Ptrs.ReuseGenerationCounterAddress = reinterpret_cast<uint64_t>(CTX->ReuseGenerationCounter);
+    Ptrs.ThreadStopHandlerSpillSRA = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(ThreadStopHandlerAddressSpillSRA)));
+    Ptrs.ThreadPauseHandlerSpillSRA = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(ThreadPauseHandlerAddressSpillSRA)));
+    Ptrs.GuestSignal_SIGILL = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(GuestSignal_SIGILL)));
+    Ptrs.GuestSignal_SIGTRAP = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(GuestSignal_SIGTRAP)));
+    Ptrs.GuestSignal_SIGSEGV = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(GuestSignal_SIGSEGV)));
+    Ptrs.SignalReturnHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(SignalHandlerReturnAddress)));
+    Ptrs.SignalReturnHandlerRT = reinterpret_cast<uint64_t>(FEXCore::Allocator::GetExecutableAddress(
+        reinterpret_cast<void*>(SignalHandlerReturnAddressRT)));
+    Ptrs.LUDIVHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(LUDIVHandlerAddress)));
+    Ptrs.LDIVHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(LDIVHandlerAddress)));
+    Ptrs.F64SinHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64SinHandlerAddress)));
+    Ptrs.F64CosHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64CosHandlerAddress)));
+    Ptrs.F64TanHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64TanHandlerAddress)));
+    Ptrs.F64F2XM1Handler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64F2XM1HandlerAddress)));
+    Ptrs.F64ScaleHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64ScaleHandlerAddress)));
+    Ptrs.F64AtanHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64AtanHandlerAddress)));
+    Ptrs.F64FYL2XHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64FYL2XHandlerAddress)));
+    Ptrs.F64FYL2XP1Handler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64FYL2XP1HandlerAddress)));
+    Ptrs.F64FPREMHandler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64FPREMHandlerAddress)));
+    Ptrs.F64FPREM1Handler = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(F64FPREM1HandlerAddress)));
 
     // Fill in the fallback handlers
     InterpreterOps::FillFallbackIndexPointers(Ptrs.FallbackHandlerPointers, &ABIPointers[0]);
   }
 }
 
+uint64_t Dispatcher::GetExitFunctionLinkerAddress() const {
+  return reinterpret_cast<uint64_t>(
+      FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(ExitFunctionLinkerAddress)));
+}
+
+Dispatcher::DiagnosticAddresses Dispatcher::GetDiagnosticAddresses() const {
+  return DiagnosticAddresses {
+    .DispatchPtr = reinterpret_cast<uint64_t>(DispatchPtr),
+    .Start = Start,
+    .End = End,
+    .ExitFunctionLinkerAddress = GetExitFunctionLinkerAddress(),
+    .DispatcherLoopTopAddress = reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(AbsoluteLoopTopAddress))),
+  };
+}
+
 SignalDelegatorConfig Dispatcher::MakeSignalDelegatorConfig() const {
   // PF/AF are the final two SRA registers. We only want GPRs
   const auto GPRCount = uint16_t(StaticRegisters.size() - 2);
@@ -2670,22 +2870,30 @@ SignalDelegatorConfig Dispatcher::MakeSignalDelegatorConfig() const {
     return Mapping;
   };
 
+  // These addresses are all consumed by the signal delegator to recognize/redirect a faulting
+  // or resuming thread's PC, which on iOS is always an executable (RX) address, so each must be
+  // translated the same way as the InitThreadPointers targets above.
+  const auto ExecAddr = [](uint64_t WriteAddr) -> uint64_t {
+    return reinterpret_cast<uint64_t>(
+        FEXCore::Allocator::GetExecutableAddress(reinterpret_cast<void*>(WriteAddr)));
+  };
+
   return FEXCore::SignalDelegatorConfig {
     .DispatcherBegin = Start,
     .DispatcherEnd = End,
 
-    .AbsoluteLoopTopAddress = AbsoluteLoopTopAddress,
-    .AbsoluteLoopTopAddressFillSRA = AbsoluteLoopTopAddressFillSRA,
-    .SignalHandlerReturnAddress = SignalHandlerReturnAddress,
-    .SignalHandlerReturnAddressRT = SignalHandlerReturnAddressRT,
+    .AbsoluteLoopTopAddress = ExecAddr(AbsoluteLoopTopAddress),
+    .AbsoluteLoopTopAddressFillSRA = ExecAddr(AbsoluteLoopTopAddressFillSRA),
+    .SignalHandlerReturnAddress = ExecAddr(SignalHandlerReturnAddress),
+    .SignalHandlerReturnAddressRT = ExecAddr(SignalHandlerReturnAddressRT),
 
-    .PauseReturnInstruction = PauseReturnInstruction,
-    .ThreadPauseHandlerAddressSpillSRA = ThreadPauseHandlerAddressSpillSRA,
-    .ThreadPauseHandlerAddress = ThreadPauseHandlerAddress,
+    .PauseReturnInstruction = ExecAddr(PauseReturnInstruction),
+    .ThreadPauseHandlerAddressSpillSRA = ExecAddr(ThreadPauseHandlerAddressSpillSRA),
+    .ThreadPauseHandlerAddress = ExecAddr(ThreadPauseHandlerAddress),
 
     // Stop handlers.
-    .ThreadStopHandlerAddressSpillSRA = ThreadStopHandlerAddressSpillSRA,
-    .ThreadStopHandlerAddress = ThreadStopHandlerAddress,
+    .ThreadStopHandlerAddressSpillSRA = ExecAddr(ThreadStopHandlerAddressSpillSRA),
+    .ThreadStopHandlerAddress = ExecAddr(ThreadStopHandlerAddress),
 
     // SRA information.
     .SRAGPRCount = GPRCount,
diff --git a/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.h b/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.h
index b0f3d7a..a5e1be4 100644
--- a/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.h
+++ b/FEXCore/Source/Interface/Core/Dispatcher/Dispatcher.h
@@ -55,9 +55,35 @@ public:
   }
 #endif
 
-  uint64_t GetExitFunctionLinkerAddress() const {
-    return ExitFunctionLinkerAddress;
-  }
+  // Defined in Dispatcher.cpp (not inline here) since it needs FEXCore::Allocator's
+  // GetExecutableAddress, which AllocatorHooks.h isn't included in this header. This value
+  // gets embedded as an absolute literal into every compiled guest block (see
+  // Arm64Relocations.cpp's SYMBOL_LITERAL_EXITFUNCTION_LINKER) and later branched to as the
+  // block-linking exit stub, so on iOS it must be the executable (RX) address like every
+  // other escaping absolute pointer here -- ExitFunctionLinkerAddress itself is the raw
+  // write-side value captured via GetCursorAddress() during EmitDispatcher().
+  uint64_t GetExitFunctionLinkerAddress() const;
+
+  // Crash-diagnostic only: the dispatcher's own known-good, already-RX-translated entry
+  // addresses, for direct comparison against whatever is actually stored in a live thread's
+  // CpuStateFrame::Pointers at the moment of a crash (see BachataDumpDispatcherState in
+  // fex_guest_engine.cpp). DispatchPtr is the dispatcher's function entry point (its very
+  // first emitted byte); DispatcherLoopTopAddress is a separate label further into the same
+  // generated code, past the prologue -- these are two genuinely different addresses by
+  // construction (confirmed: ~0xa0 bytes apart in EmitDispatcher, matching AbsoluteLoopTop's
+  // Bind() coming after several prologue instructions), not two copies of the same value.
+  // Compare each live Pointers field against its OWN matching field here, never against
+  // DispatchPtr for anything but Pointers.DispatchPtr itself -- an earlier version of this
+  // comparison did exactly that (Live.DispatcherLoopTop vs Known.DispatchPtr) and reported a
+  // permanent, meaningless "MISMATCH" on every single run, real corruption or not.
+  struct DiagnosticAddresses {
+    uint64_t DispatchPtr;
+    uint64_t Start;
+    uint64_t End;
+    uint64_t ExitFunctionLinkerAddress;
+    uint64_t DispatcherLoopTopAddress;
+  };
+  DiagnosticAddresses GetDiagnosticAddresses() const;
 
   SignalDelegatorConfig MakeSignalDelegatorConfig() const;
 
@@ -76,12 +102,16 @@ private:
   uint64_t ThreadStopHandlerAddress {};
   uint64_t ThreadStopHandlerAddressSpillSRA {};
   uint64_t AbsoluteLoopTopAddress {};
+  // See JITPointers::DispatcherBlockLookupRangeEnd (CoreState.h) for what this bounds.
+  uint64_t DispatcherBlockLookupRangeEndAddress {};
   uint64_t AbsoluteLoopTopAddressFillSRA {};
   uint64_t AbsoluteLoopTopAddressEnterEC {};
   uint64_t AbsoluteLoopTopAddressEnterECFillSRA {};
   uint64_t ThreadPauseHandlerAddress {};
   uint64_t ThreadPauseHandlerAddressSpillSRA {};
   uint64_t ExitFunctionLinkerAddress {};
+  // See JITPointers::DispatcherBlockLookupRangeEnd (CoreState.h) for what this bounds.
+  uint64_t ExitFunctionLinkerRangeEndAddress {};
   uint64_t SignalHandlerReturnAddress {};
   uint64_t SignalHandlerReturnAddressRT {};
   uint64_t GuestSignal_SIGILL {};
diff --git a/FEXCore/Source/Interface/Core/JIT/ALUOps.cpp b/FEXCore/Source/Interface/Core/JIT/ALUOps.cpp
index 7946ccd..9eb443d 100644
--- a/FEXCore/Source/Interface/Core/JIT/ALUOps.cpp
+++ b/FEXCore/Source/Interface/Core/JIT/ALUOps.cpp
@@ -11,6 +11,8 @@ $end_info$
 #include "Interface/Core/JIT/JITClass.h"
 #include "Interface/IR/Passes/RegisterAllocationPass.h"
 
+#include <FEXCore/Utils/LogManager.h>
+
 namespace FEXCore::CPU {
 
 #define DEF_BINOP_WITH_CONSTANT(FEXOp, VarOp, ConstOp)                                    \
@@ -61,6 +63,20 @@ DEF_OP(EntrypointOffset) {
     Mask = 0xFFFF'FFFFULL;
   }
 
+  // A RIP-relative guest memory operand (the overwhelmingly common use of this IR op) resolving
+  // to exactly 0 is never legitimate -- no real module is ever loaded at guest address 0, so a
+  // real game's own RIP-relative reference can never actually target it. Flagging this
+  // unconditionally (not gated behind a debug log level) since it's rare and, if it fires, is
+  // by itself strong evidence of a compile-time miscalculation in Entry/Offset for this
+  // specific block -- worth capturing at the exact moment it happens rather than only inferring
+  // it after the fact from a "write to 0x0" crash dump, which doesn't say which of the many
+  // memory operands in a block was the wrong one.
+  if ((Constant & Mask) == 0) {
+    LogMan::Msg::EFmt("BACHATA_ENTRYPOINT_OFFSET_ZERO: Entry={:#x} Offset={:#x} OpSize={} -> "
+                      "Constant=0 (suspicious -- no real guest address should ever resolve here)",
+                      Entry, Op->Offset, static_cast<int>(OpSize));
+  }
+
   InsertGuestRIPMove(GetReg(Node), Constant & Mask);
 }
 
diff --git a/FEXCore/Source/Interface/Core/JIT/BranchOps.cpp b/FEXCore/Source/Interface/Core/JIT/BranchOps.cpp
index d555e06..92d483e 100644
--- a/FEXCore/Source/Interface/Core/JIT/BranchOps.cpp
+++ b/FEXCore/Source/Interface/Core/JIT/BranchOps.cpp
@@ -189,6 +189,33 @@ DEF_OP(ExitFunction) {
     ARMEmitter::ForwardLabel SkipFullLookup;
     auto RipReg = GetReg(Op->NewRIP);
 
+    // Snapshot the reuse-generation counter before either lookup below (call-ret shadow stack or
+    // L1 cache) reads a cached HostCode -- same mechanism as DispatcherLoopTop's own block lookup
+    // and ExitFunctionLinker (see JITPointers::ReuseGenerationCounterAddress's own comment,
+    // CoreState.h, and SafepointSignalHandler's, fex_guest_engine.cpp, for the full story). This
+    // path handles genuinely computed/indirect guest branches -- register-indirect calls/jumps,
+    // returns, and memory-indirect jumps like x86 `jmp [mem]` -- a third instance of the same
+    // lock-free read-then-branch shape, with its own separate cache, that neither of those two
+    // fixes reached. Confirmed on-device as the actual remaining source of the reuse-in-place
+    // crash: the guest instruction at the fault RIP was exactly `jmp [rip+disp]`, a case that can
+    // only ever route through this indirect path, never the other two. TMP3 is unused elsewhere
+    // in this function's indirect-branch handling, so it's safe as the snapshot register across
+    // everything below, regardless of which of the three branch kinds (call/return/jump) is taken.
+    //
+    // ComputedBranchGenerationUnwired guards ONLY the dereference on the next line (can't safely
+    // dereference a null ReuseGenerationCounterAddress) -- it must never skip past the shadow
+    // stack / L1 lookup logic below. An earlier version of this check used one label for both
+    // "unwired, skip the whole mechanism" and "skip just the re-check at the end", which for the
+    // *unwired* case jumped straight past the entire original lookup, leaving TMP2 completely
+    // uninitialized before the final call/return/jump -- confirmed on-device as a new,
+    // unrelated-looking crash (garbage register values, fault address 0x0) that had nothing to
+    // do with JIT reuse at all, introduced by this fix itself.
+    ARMEmitter::ForwardLabel ComputedBranchGenerationUnwired;
+    ldr(TMP3, STATE_PTR(CpuStateFrame, Pointers.ReuseGenerationCounterAddress));
+    (void)cbz(ARMEmitter::Size::i64Bit, TMP3, &ComputedBranchGenerationUnwired);
+    ldr(TMP3, TMP3, 0);
+    (void)Bind(&ComputedBranchGenerationUnwired);
+
     if (Op->Hint == IR::BranchHint::Return) {
       // First try to pop from the call-ret stack, otherwise follow the normal path (but ending in a ret)
       ldp<ARMEmitter::IndexType::POST>(TMP1, TMP2, REG_CALLRET_SP, 0x10);
@@ -213,6 +240,30 @@ DEF_OP(ExitFunction) {
     str(RipReg.X(), STATE, offsetof(FEXCore::Core::CpuStateFrame, State.rip));
 
     (void)Bind(&SkipFullLookup);
+
+    // Re-check right before trusting TMP2 (a cache-hit HostCode from either lookup above, or --
+    // harmlessly redundant in that specific case -- DispatcherLoopTop's own address from the
+    // fallback just above, converged to the same TMP2 by this point): if the generation changed
+    // since the snapshot, redirect to DispatcherLoopTop instead of whatever TMP2 currently holds.
+    // Safe unconditionally, same reasoning as the other two checks of this kind -- and TMP1 is
+    // free to reuse here regardless of which path led to this point (none of the three branch
+    // kinds below rely on its value surviving from earlier in this function).
+    //
+    // Re-loads and re-checks ReuseGenerationCounterAddress itself here rather than reusing TMP3's
+    // zero-ness, deliberately: TMP3 legitimately reads as 0 both when unwired (nothing to compare
+    // against) AND when wired but no reuse cycle has happened yet this session (a real, valid
+    // snapshot that just happens to be 0) -- conflating those would skip this whole check for
+    // that second, perfectly normal case.
+    ARMEmitter::ForwardLabel SkipComputedBranchRecheck;
+    ldr(TMP1, STATE_PTR(CpuStateFrame, Pointers.ReuseGenerationCounterAddress));
+    (void)cbz(ARMEmitter::Size::i64Bit, TMP1, &SkipComputedBranchRecheck);
+    ldr(TMP1, TMP1, 0);
+    sub(TMP1, TMP1, TMP3);
+    (void)cbz(ARMEmitter::Size::i64Bit, TMP1, &SkipComputedBranchRecheck);
+    ldr(TMP2, STATE_PTR(CpuStateFrame, Pointers.DispatcherLoopTop));
+    str(RipReg.X(), STATE, offsetof(FEXCore::Core::CpuStateFrame, State.rip));
+
+    (void)Bind(&SkipComputedBranchRecheck);
     if (Op->Hint == IR::BranchHint::Call) {
       ARMEmitter::ForwardLabel l_CallReturn;
       if (!Op->CallReturnBlock.IsInvalid()) {
diff --git a/FEXCore/Source/Interface/Core/JIT/JIT.cpp b/FEXCore/Source/Interface/Core/JIT/JIT.cpp
index b872b6f..a46d99b 100644
--- a/FEXCore/Source/Interface/Core/JIT/JIT.cpp
+++ b/FEXCore/Source/Interface/Core/JIT/JIT.cpp
@@ -35,6 +35,7 @@ $end_info$
 #include <FEXCore/Utils/TypeDefines.h>
 #include <FEXCore/HLE/SyscallHandler.h>
 
+#include <atomic>
 #include <cstdio>
 #include <cstring>
 #include <unistd.h>
@@ -519,7 +520,15 @@ static void DirectBlockDelinker(FEXCore::Context::ExitFunctionLinkData* Record,
     BranchEmit.b(BranchOffset);
   }
 
-  std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(CallerAddress)).store(BranchInst, std::memory_order::relaxed);
+  // CallerAddress was derived from Record, which is itself captured at runtime from the LR of
+  // the `blr` inside the currently-*executing* jump thunk -- i.e. it's always the executable
+  // (RX) address on iOS's dual-mapped scheme, which may not be writable at all. Translate back
+  // to the writable (RW) alias before storing, then clear the icache at both (see JIT.cpp's
+  // CompileCode ClearICache calls for why both sides need it: the write dirties a cache line
+  // tagged by the RW VA, while the RX VA is the one actually fetched from).
+  void* WriteCallerAddress = FEXCore::Allocator::GetWritableAddress(reinterpret_cast<void*>(CallerAddress));
+  std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(WriteCallerAddress)).store(BranchInst, std::memory_order::relaxed);
+  ARMEmitter::Emitter::ClearICache(WriteCallerAddress, 4);
   ARMEmitter::Emitter::ClearICache(reinterpret_cast<void*>(CallerAddress), 4);
 }
 
@@ -533,7 +542,12 @@ static void IndirectBlockDelinker(FEXCore::Context::ExitFunctionLinkData* Record
   // Restore branch +2 instructions to jump to the linker block
   BranchEmit.b(0x2);
 
-  std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(JumpThunkStartAddress)).store(BranchInst, std::memory_order::relaxed);
+  // See DirectBlockDelinker above: JumpThunkStartAddress is RX-derived (from Record), needs
+  // the same RW translation before writing, and both sides need clearing afterward.
+  void* WriteJumpThunkStartAddress =
+    FEXCore::Allocator::GetWritableAddress(reinterpret_cast<void*>(JumpThunkStartAddress));
+  std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(WriteJumpThunkStartAddress)).store(BranchInst, std::memory_order::relaxed);
+  ARMEmitter::Emitter::ClearICache(WriteJumpThunkStartAddress, 4);
   ARMEmitter::Emitter::ClearICache(reinterpret_cast<void*>(JumpThunkStartAddress), 4);
 
   // No need to reset HostCode here as the exit linker pointer is stored separately, and if the block is relinked it will be updated.
@@ -561,6 +575,19 @@ uint64_t Arm64JITCore::ExitFunctionLink(FEXCore::Core::CpuStateFrame* Frame, FEX
       auto CodeBuffer = static_cast<Arm64JITCore*>(Thread->CPUBackend.get())->CurrentCodeBuffer;
       HostCode = static_cast<Context::ContextImpl*>(Thread->CTX)->CompileBlock(Frame, GuestRip, 0);
       if (Thread->LookupCache->Shared != CodeBuffer->LookupCache.get()) {
+        // See the final return's own comment, below, for why this is set here too -- the
+        // caller's ExitFunctionLinker (Dispatcher.cpp) branches to whatever HostCode this
+        // function returns with no lock or synchronization of its own, and a safepoint-paused
+        // thread caught in that window needs State.rip to already reflect this target for a
+        // DispatcherLoopTop redirect to land anywhere sane. Also record the current
+        // reuse-generation (see the final return's comment) for the same caller's own,
+        // non-signal-dependent staleness check.
+        Frame->State.rip = GuestRip;
+        if (Frame->Pointers.ReuseGenerationCounterAddress) {
+          Frame->ExitFunctionLinkValidatedGeneration =
+            reinterpret_cast<std::atomic<uint64_t>*>(Frame->Pointers.ReuseGenerationCounterAddress)
+              ->load(std::memory_order_acquire);
+        }
         return HostCode;
       }
     }
@@ -588,6 +615,30 @@ uint64_t Arm64JITCore::ExitFunctionLink(FEXCore::Core::CpuStateFrame* Frame, FEX
   // Guard the LookupCache lock with the code invalidation mutex, to avoid issues with forking
   auto lk_inval = GuardSignalDeferringSection<std::shared_lock>(static_cast<Context::ContextImpl*>(Thread->CTX)->CodeInvalidationMutex, Thread);
 
+  // Re-validate HostCode now that we hold CodeInvalidationMutex again -- it was captured earlier
+  // (either FindBlock's fast-path hit above, or CompileBlock's return value on the slow path)
+  // while that lock was held only briefly, and an iOS reuse-in-place cycle (NewCodeBuffer=false)
+  // could have run entirely within the gap since then. Unlike a NewCodeBuffer=true reuse,
+  // reuse-in-place clears BlockList/BlockLinks without swapping the LookupCache object itself,
+  // so it's invisible to the "Thread->LookupCache->Shared != CodeBuffer->LookupCache.get()" check
+  // the slow path already does above. Worse, neither that check nor the safepoint mechanism
+  // protects a value already captured into this function's own HostCode local: a thread can be
+  // safepoint-paused *after* reading a since-invalidated HostCode but *before* ever writing it
+  // anywhere, and on resume it has no way to know the value it's still holding went stale while
+  // it was frozen -- the pause only ever protected memory, not a local variable already sitting
+  // in a register. Confirmed on-device as the remaining source of the exact same
+  // "instruction-fetch fault, not inside any currently-live JIT allocation" crash even after the
+  // safepoint's own hold window and CodeInvalidationMutex acquisition ordering were separately
+  // fixed. A fresh FindBlock() here is cheap insurance (this must run before AcquireWriteLock()
+  // below -- FindBlock takes its own read lock on the same underlying mutex GuestToHostMap::Lock,
+  // which would deadlock against that write lock from this same thread); if it disagrees with
+  // what we're still holding, re-resolve properly instead of patching a branch to a target
+  // that's already gone.
+  if (auto Current = Thread->LookupCache->FindBlock(Thread, GuestRip); Current != HostCode) {
+    HostCode = Current ? Current : static_cast<Context::ContextImpl*>(Thread->CTX)->CompileBlock(Frame, GuestRip, 0);
+    BranchOffset = HostCode / 4 - CallerAddress / 4;
+  }
+
   // Lock here is necessary to prevent simultaneous linking and delinking
   auto lk = Thread->LookupCache->AcquireWriteLock();
 
@@ -610,25 +661,63 @@ uint64_t Arm64JITCore::ExitFunctionLink(FEXCore::Core::CpuStateFrame* Frame, FEX
         GuestRip, Record, [](FEXCore::Context::ExitFunctionLinkData* Record) { DirectBlockDelinker(Record, false); }, lk);
     }
 
-    std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(CallerAddress)).store(BranchInst, std::memory_order::relaxed);
+    // CallerAddress is RX-derived (from Record, captured off the currently-executing thunk's
+    // return address) -- translate to the writable alias before storing, same as
+    // DirectBlockDelinker/IndirectBlockDelinker above, and clear the icache at both sides.
+    void* WriteCallerAddress = FEXCore::Allocator::GetWritableAddress(reinterpret_cast<void*>(CallerAddress));
+    std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(WriteCallerAddress)).store(BranchInst, std::memory_order::relaxed);
+    ARMEmitter::Emitter::ClearICache(WriteCallerAddress, 4);
     ARMEmitter::Emitter::ClearICache(reinterpret_cast<void*>(CallerAddress), 4);
   } else {
     // This case is common between calls and jumps as the thunk callsite can be left untouched.
-    std::atomic_ref<uint64_t>(Record->HostCode).store(HostCode, std::memory_order::seq_cst);
+    // Record itself lives inside the JIT buffer (the ExitFunctionLinkData struct emitted right
+    // in the code stream, see JIT.cpp's PendingJumpThunks loop) and is RX-derived same as
+    // JumpThunkStartAddress/CallerAddress -- Record->HostCode needs the same RW translation.
+    void* WriteRecord = FEXCore::Allocator::GetWritableAddress(Record);
+    auto* WriteHostCode = &reinterpret_cast<FEXCore::Context::ExitFunctionLinkData*>(WriteRecord)->HostCode;
+    std::atomic_ref<uint64_t>(*WriteHostCode).store(HostCode, std::memory_order::seq_cst);
 #ifdef ARCHITECTURE_arm64
     // Make memory write visible to other threads reading the same location
-    asm volatile("dc cvau, %0; dsb ish" : : "r"(Record->HostCode) :);
+    asm volatile("dc cvau, %0; dsb ish" : : "r"(WriteHostCode) :);
+    asm volatile("dc cvau, %0; dsb ish" : : "r"(&Record->HostCode) :);
 #endif
 
     uint32_t LdrInst = 0;
     ARMEmitter::Emitter LdrEmit(reinterpret_cast<uint8_t*>(&LdrInst), 4);
     LdrEmit.ldr(TMP1, reinterpret_cast<uint64_t>(&Record->HostCode) - JumpThunkStartAddress);
-    std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(JumpThunkStartAddress)).store(LdrInst, std::memory_order::relaxed);
+    void* WriteJumpThunkStartAddress =
+      FEXCore::Allocator::GetWritableAddress(reinterpret_cast<void*>(JumpThunkStartAddress));
+    std::atomic_ref<uint32_t>(*reinterpret_cast<uint32_t*>(WriteJumpThunkStartAddress)).store(LdrInst, std::memory_order::relaxed);
+    ARMEmitter::Emitter::ClearICache(WriteJumpThunkStartAddress, 4);
     ARMEmitter::Emitter::ClearICache(reinterpret_cast<void*>(JumpThunkStartAddress), 4);
 
     Thread->LookupCache->AddBlockLink(GuestRip, Record, IndirectBlockDelinker, lk);
   }
 
+  // The caller (ExitFunctionLinker, Dispatcher.cpp) takes this return value in TMP1 and, after a
+  // few more instructions (spilling regs, an isb, no further lock or synchronization of any
+  // kind), branches straight to it -- a second, separate instance of the same class of bug the
+  // HostCode re-validation above this function already fixed, just living in the assembly glue
+  // around this call instead of inside it. A thread safepoint-paused anywhere in that window
+  // resumes still holding this exact value with the branch to it still pending; the
+  // SafepointSignalHandler (fex_guest_engine.cpp) redirects such a thread back to
+  // DispatcherLoopTop, which needs State.rip to already be the real target -- match the TFSet
+  // branch above, which has done this from the start for exactly this reason.
+  //
+  // Being paused in time isn't guaranteed, though: signal delivery has real, nonzero latency,
+  // and if this whole window (from here to ExitFunctionLinker's own final branch) finishes
+  // faster than that latency, this thread executes the branch having never been signaled at
+  // all -- confirmed on-device as still crashing with the signal-based redirect alone in place.
+  // ExitFunctionLinkValidatedGeneration is the fallback: record the reuse-generation counter's
+  // value right now, at the moment HostCode is confirmed fresh, so ExitFunctionLinker can
+  // re-read the live counter right before its branch and catch a reuse that completed in that
+  // gap even if the signal missed it entirely.
+  Frame->State.rip = GuestRip;
+  if (Frame->Pointers.ReuseGenerationCounterAddress) {
+    Frame->ExitFunctionLinkValidatedGeneration =
+      reinterpret_cast<std::atomic<uint64_t>*>(Frame->Pointers.ReuseGenerationCounterAddress)
+        ->load(std::memory_order_acquire);
+  }
   return HostCode;
 }
 
@@ -702,11 +791,30 @@ void Arm64JITCore::ClearCache() {
   auto PrevCodeBuffer = CurrentCodeBuffer;
   auto lk = PrevCodeBuffer->LookupCache->AcquireWriteLock();
 
+  // Delink direct branches other already-compiled code patched straight into blocks living in
+  // PrevCodeBuffer -- ChangeGuestToHostMapping below only clears this thread's own L1/L2 and
+  // swaps Shared to the new buffer, it never touches PrevCodeBuffer's own BlockLinks. Once
+  // PrevCodeBuffer's memory is eventually reclaimed/reused (this build's iOS JIT allocations
+  // are a small, StikDebug-count-limited shared pool -- see ClearCodeCache's NewCodeBuffer=false
+  // path, Core.cpp), any surviving direct patch into it becomes a landmine: a jump to memory
+  // that's since gone from "stale content" to "not part of any live allocation at all".
+  // Confirmed on-device: a crash with exactly that signature (LR inside the dispatcher's
+  // ExitFunctionLinker trampoline, PC entirely outside every current buffer) persisted even
+  // after the reuse-in-place path's own cross-thread cache sync was fully fixed and verified
+  // working -- this fresh-allocation path was the one remaining place still skipping delinking.
+  PrevCodeBuffer->LookupCache->ClearCache(lk);
+
   auto CodeBuffer = GetEmptyCodeBuffer();
   SetBuffer(CodeBuffer->Ptr, CodeBuffer->AllocatedSize);
   EmitDetectionString();
 
   ThreadState->LookupCache->ChangeGuestToHostMapping(*PrevCodeBuffer, *CurrentCodeBuffer->LookupCache, lk);
+  // Sibling guest threads' independent L1/L2 caches also need to be reached here, same as the
+  // reuse-in-place path -- this fresh allocation just as validly invalidates their stale
+  // entries as an in-place reuse does. See OnBufferReusedInPlace's own comment (Context.h).
+  if (CTX->OnBufferReusedInPlace) {
+    CTX->OnBufferReusedInPlace(ThreadState, lk);
+  }
 }
 
 Arm64JITCore::~Arm64JITCore() {}
@@ -1110,6 +1218,14 @@ CPUBackend::CompiledCode Arm64JITCore::CompileCode(uint64_t Entry, uint64_t Size
   {
     auto CodeBufferLock = std::unique_lock {CodeBuffers.CodeBufferWriteMutex};
 
+    // Set below, on the iOS reuse-in-place path only, when this compile actually took
+    // CodeInvalidationMutex exclusively and opened a BeginBufferInvalidationSafepoint pause --
+    // see the matching unlock()/EndBufferInvalidationSafepoint call near this function's memcpy
+    // below, and BeginBufferInvalidationSafepoint's own comment (Context.h) for why both need to
+    // stay held that far past the metadata clear, and why the mutex must be acquired before the
+    // pause is opened rather than after.
+    bool HeldReuseSafepoint = false;
+
     // Query size of generated code
     const auto TempSize = GetCursorOffset();
 
@@ -1128,7 +1244,73 @@ CPUBackend::CompiledCode Arm64JITCore::CompileCode(uint64_t Entry, uint64_t Size
       SetCursorOffset(CodeBuffers.LatestOffset);
       Align16B();
       if ((GetCursorOffset() + TempSize) > CurrentCodeBuffer->UsableSize()) {
+#if defined(__APPLE__) && TARGET_OS_IPHONE
+        // NewCodeBuffer=true (the default) requests a brand new dual-mapped allocation from
+        // StikDebug's external BreakpointJIT mechanism every time the cache fills, which fails
+        // on a session's 3rd+ such request (confirmed on-device, independent of the requested
+        // size -- see CPUBackend.cpp's iOS MAX_CODE_SIZE comment). NewCodeBuffer=false reuses
+        // the same already-granted buffer in place instead, avoiding that failure entirely.
+        // This was tried once before and reverted: GuestToHostMap::ClearCache
+        // (LookupCache.cpp) discarded its BlockLinks map without ever calling each entry's
+        // delinker, so already-patched direct branches in other, non-evicted code kept
+        // pointing at addresses inside the reused region -- once that physical memory got
+        // overwritten with new compiled code, re-taking one of those stale links landed on
+        // unrelated bytes, and the resulting fault's own recovery path could only fix the
+        // register for one instruction, not the stale cached target, so the same handful of
+        // addresses re-faulted forever (confirmed on-device: 7,000,000+ recoveries across
+        // only 8 distinct addresses, no crash, no progress). ClearCache now properly severs
+        // every link before discarding them, the same way Erase() already did for a single
+        // address, which is the actual fix -- re-enabling reuse-in-place here now that the
+        // real bug underneath it is fixed, rather than working around it by avoiding this
+        // path altogether.
+        //
+        // ClearCodeCache's NewCodeBuffer=false branch (Core.cpp) requires the caller to already
+        // hold CodeInvalidationMutex exclusively before calling in, to exclude any other thread
+        // that might currently be *executing* out of the buffer being reused (not just compiling
+        // into it -- execution of already-JIT'd code never touches this mutex at all, so this is
+        // the only guard available). This call site is reached from deep inside CompileBlock
+        // (Core.cpp), which already holds that same mutex *shared* for its entire duration
+        // (GuardSignalDeferringSection<std::shared_lock>) -- this mutex doesn't support
+        // recursive or shared-to-exclusive-upgrade locking, so acquiring the exclusive lock
+        // without first releasing that shared reference would deadlock against ourselves.
+        // Release it manually here and restore it (see below, past the memcpy), bypassing the
+        // RAII wrapper CompileBlock's local `lk` owns (its own eventual unlock_shared() in that
+        // caller's destructor stays correctly balanced as long as this thread holds exactly one
+        // shared reference again by the time this function returns, which the sequence below
+        // guarantees) -- WritePriorityMutex::Mutex doesn't track *which* call site (de)acquired
+        // it, only the net reader/writer counts, so this is safe purely at the mutex level.
+        //
+        // Acquire the exclusive lock BEFORE opening the safepoint pause, not after -- see
+        // BeginBufferInvalidationSafepoint's own comment (Context.h) for the deadlock this
+        // ordering avoids: pausing every other thread first can freeze one of them mid-hold of
+        // this same mutex (shared, from inside its own CompileBlock/ExitFunctionLink), and it can
+        // never release that hold until its pause lifts, which can't happen until we finish here
+        // -- confirmed on-device as an indefinite hang with the safepoint itself reporting every
+        // other thread successfully paused. Taking the exclusive lock first sidesteps this
+        // entirely: once acquired, no thread can currently hold it shared, so freezing everyone
+        // afterward can't catch one mid-hold.
+        //
+        // The pause itself (BeginBufferInvalidationSafepoint/EndBufferInvalidationSafepoint) is
+        // owned here, not inside ClearCodeCache, because clearing the LookupCache metadata alone
+        // doesn't make the buffer safe to resume into -- the bytes a paused-then-resumed thread
+        // was already executing stay physically unchanged until this function's memcpy below
+        // actually overwrites them. Releasing the pause right after the metadata clear (as
+        // ClearCodeCache used to do on its own) leaves a window where a just-resumed thread can
+        // race that memcpy into the exact memory it's still running through -- confirmed on-device
+        // as two different guest threads instruction-fetch-faulting at addresses only 0x80 apart,
+        // both landing in memory the LookupCache had already forgotten. Held open from here
+        // through the memcpy closes that window; see the matching End call below.
+        auto& InvalidationMutex = CTX->GetCodeInvalidationMutex();
+        InvalidationMutex.unlock_shared();
+        InvalidationMutex.lock();
+        if (CTX->BeginBufferInvalidationSafepoint) {
+          CTX->BeginBufferInvalidationSafepoint(ThreadState);
+          HeldReuseSafepoint = true;
+        }
+        CTX->ClearCodeCache(ThreadState, false);
+#else
         CTX->ClearCodeCache(ThreadState);
+#endif
       }
 
       CodeBuffers.LatestOffset = GetCursorOffset();
@@ -1139,6 +1321,12 @@ CPUBackend::CompiledCode Arm64JITCore::CompileCode(uint64_t Entry, uint64_t Size
     CodeData.BlockBegin += Delta;
     for (auto& EntryPoint : CodeData.EntryPoints) {
       EntryPoint.second += Delta;
+      // EntryPoints' values escape this function (see ContextImpl::CompileBlock in Core.cpp,
+      // which returns one directly as the uintptr_t the dispatcher branches to via
+      // "Ptr = CTX->CompileBlock(RIP); Ptr();", and they're inserted into the LookupCache other
+      // JIT'd code reads and branches to indirectly) -- unlike BlockBegin below, which is only
+      // ever used for symbol/debug bookkeeping, never branched to, so it stays a write address.
+      EntryPoint.second = reinterpret_cast<uint8_t*>(FEXCore::Allocator::GetExecutableAddress(EntryPoint.second));
     }
     CodeBegin += Delta;
 
@@ -1151,11 +1339,39 @@ CPUBackend::CompiledCode Arm64JITCore::CompileCode(uint64_t Entry, uint64_t Size
     SetCursorOffset(CodeBuffers.LatestOffset + TempSize);
 
     CodeBuffers.LatestOffset = GetCursorOffset();
+
+    // Memory this compile reused is fully repopulated now -- safe for any thread paused above to
+    // resume into. See the matching BeginBufferInvalidationSafepoint call and its comment, above.
+    // Release CodeInvalidationMutex (acquired exclusive, above, before the pause was opened) only
+    // after ending the pause, then restore this thread's own shared reference to it, matching the
+    // shared reference it held on entry to this reuse-in-place branch.
+    if (HeldReuseSafepoint) {
+      CTX->EndBufferInvalidationSafepoint(ThreadState);
+      auto& InvalidationMutex = CTX->GetCodeInvalidationMutex();
+      InvalidationMutex.unlock();
+      InvalidationMutex.lock_shared();
+    }
   }
 
   TempAllocator.DelayedDisownBuffer();
 
+  // CodeBegin is a write (RW) address (see the comment on EntryPoint.second above -- unlike that
+  // one, this pointer was never translated since it's normally "only used for symbol/debug
+  // bookkeeping"). The actual memcpy of freshly-compiled bytes above happened through this RW
+  // alias, so on iOS's dual-mapped scheme (RW and RX are different VAs backed by the same
+  // physical pages) two separate cache-maintenance concerns apply, and both addresses are needed:
+  //  1. The CPU's data cache may have dirtied a line tagged by the RW VA (the one actually
+  //     written through) that a clean/invalidate issued only at the RX VA won't necessarily
+  //     reach -- a classic VIPT synonym hazard for two virtual aliases of one physical page.
+  //     Clearing at CodeBegin (RW) first flushes that dirtied line to the point of unification.
+  //  2. The CPU's instruction cache may hold stale entries under the RX VA's own tag (e.g. from
+  //     a previous occupant of this buffer slot), which only a clear issued at the RX address
+  //     itself invalidates -- and RX is the alias actually fetched from when this code is later
+  //     branched to, so it must be current.
+  // Skipping either one reintroduces the same "branch into fresh code" instruction-fetch fault:
+  // RX-only leaves the RW-side dirty line unflushed; RW-only leaves the RX-side icache stale.
   ClearICache(CodeBegin, CodeOnlySize);
+  ClearICache(FEXCore::Allocator::GetExecutableAddress(CodeBegin), CodeOnlySize);
 
 #ifdef VIXL_DISASSEMBLER
   if (Disassemble() & FEXCore::Config::Disassemble::STATS) {
diff --git a/FEXCore/Source/Interface/Core/LookupCache.cpp b/FEXCore/Source/Interface/Core/LookupCache.cpp
index 0b23030..06c2ff5 100644
--- a/FEXCore/Source/Interface/Core/LookupCache.cpp
+++ b/FEXCore/Source/Interface/Core/LookupCache.cpp
@@ -102,6 +102,27 @@ void LookupCache::ClearCache(const LookupCacheWriteLockToken& lk) {
 }
 
 void GuestToHostMap::ClearCache(const LookupCacheWriteLockToken&) {
+  // Sever every recorded direct-branch link before discarding them. Erase(Address, ...) already
+  // does this correctly for a single address (walk BlockLinks, call each delinker with its
+  // HostLink), but until now this full-cache clear just replaced BlockLinks with a fresh empty
+  // map, silently dropping every entry without ever calling its delinker. On every platform that
+  // only ever grows into fresh memory on eviction (NewCodeBuffer=true, the normal case
+  // everywhere) that gap was invisible: the stale patched branches point at addresses inside the
+  // now-abandoned old buffer, which nothing valid ever branches back into again anyway. It
+  // stopped being invisible on iOS, where JIT buffer reuse-in-place (NewCodeBuffer=false) was
+  // tried as a workaround for a separate StikDebug allocation-count limitation: reusing the same
+  // memory for new compiled code, with old delinkers never having been called, left other
+  // still-referenced code's already-patched direct branches pointing at addresses that now held
+  // completely unrelated new code. HandleGuestSignal's SIGBUS-recovery paths could keep
+  // "fixing" the resulting faults' host/writable-vs-executable address one instruction at a
+  // time, but never the actual stale target underneath -- confirmed on-device as an unbounded
+  // fault loop (7,000,000+ recoveries cycling through only 8 distinct addresses, no crash, no
+  // progress). Actually severing every link here, the same way Erase() already does for one
+  // address, is the real fix for that, independent of whichever eviction mode ends up used.
+  for (auto& [Tag, Delinker] : *BlockLinks) {
+    Delinker(Tag.HostLink);
+  }
+
   // Allocate a new pointer from the BlockLinks pma again.
   BlockLinks = BlockLinks_pma->new_object<BlockLinksMapType>();
   // All code is gone, clear the block list
diff --git a/FEXCore/Source/Interface/Core/LookupCache.h b/FEXCore/Source/Interface/Core/LookupCache.h
index bf958d8..68cef7d 100644
--- a/FEXCore/Source/Interface/Core/LookupCache.h
+++ b/FEXCore/Source/Interface/Core/LookupCache.h
@@ -25,12 +25,21 @@ protected:
 };
 
 struct LookupCacheWriteLockToken : public LookupCacheBaseLockToken {
+  // Whether this token actually holds the lock -- only meaningful for a token constructed via
+  // the try_to_lock overload below (see GuestToHostMap::TryAcquireWriteLock's own comment for
+  // why that path exists). A token from the plain (blocking) constructor always owns the lock.
+  [[nodiscard]] bool Owned() const {
+    return Lock.owns_lock();
+  }
+
 private:
   // Only constructible by GuestToHostMap
   friend struct GuestToHostMap;
   LookupCacheWriteLockToken(FEXCore::Utils::WritePriorityMutex::Mutex& Mutex)
     : Lock {Mutex} {}
-  std::lock_guard<FEXCore::Utils::WritePriorityMutex::Mutex> Lock;
+  LookupCacheWriteLockToken(FEXCore::Utils::WritePriorityMutex::Mutex& Mutex, std::try_to_lock_t)
+    : Lock {Mutex, std::try_to_lock} {}
+  std::unique_lock<FEXCore::Utils::WritePriorityMutex::Mutex> Lock;
 };
 
 struct LookupCacheReadLockToken : public LookupCacheBaseLockToken {
@@ -50,6 +59,22 @@ struct GuestToHostMap {
     return LookupCacheWriteLockToken {Lock};
   }
 
+  // Non-blocking variant: check LookupCacheWriteLockToken::Owned() on the result before using
+  // it. Exists for cross-thread cache invalidation (a thread reusing the shared JIT buffer in
+  // place, clearing sibling threads' caches -- see OnBufferReusedInPlace's own comment,
+  // Context.h) where blocking here risks a real deadlock: the calling thread already holds
+  // CodeInvalidationMutex exclusively at that point, and if the target thread is itself
+  // waiting on that same mutex (e.g. mid-compile, about to take it shared) while holding this
+  // very lock, the blocking AcquireWriteLock() above would wait forever on a lock its own
+  // holder can never release. Confirmed on-device: exactly this scenario hung Minecraft's
+  // boot indefinitely with zero further progress. Skipping a thread here just leaves its L1/L2
+  // stale for one more compile cycle -- the same (already-handled) class of fault as a
+  // still-tracked-but-stale address, not a hang.
+  [[nodiscard]]
+  LookupCacheWriteLockToken TryAcquireWriteLock() {
+    return LookupCacheWriteLockToken {Lock, std::try_to_lock};
+  }
+
   [[nodiscard]]
   LookupCacheReadLockToken AcquireReadLock() {
     return LookupCacheReadLockToken {Lock};
@@ -379,6 +404,12 @@ public:
     return Shared->AcquireWriteLock();
   }
 
+  // See GuestToHostMap::TryAcquireWriteLock's own comment for why this non-blocking variant
+  // exists. Check LookupCacheWriteLockToken::Owned() on the result before using it.
+  auto TryAcquireWriteLock() {
+    return Shared->TryAcquireWriteLock();
+  }
+
 private:
   void CacheBlockMapping(uint64_t Address, const GuestToHostMap::BlockEntry& Entry, bool L1Only, const LookupCacheBaseLockToken& lk) {
     for (const auto& CodePage : Entry.CodePages) {
diff --git a/FEXCore/include/FEXCore/Core/CoreState.h b/FEXCore/include/FEXCore/Core/CoreState.h
index 7c64dd8..8e109a0 100644
--- a/FEXCore/include/FEXCore/Core/CoreState.h
+++ b/FEXCore/include/FEXCore/Core/CoreState.h
@@ -364,10 +364,33 @@ struct JITPointers {
    * @name Dispatcher pointers
    * @{ */
   uint64_t DispatcherLoopTop {};
+  // End of DispatcherLoopTop's lock-free block-cache lookup routine (the hottest path in the
+  // JIT: read a cached guest->host mapping straight out of L1/L2 with no synchronization at
+  // all, then branch directly to the cached host address). A thread interrupted anywhere in
+  // [DispatcherLoopTop, DispatcherBlockLookupRangeEnd) may already hold that cached address in
+  // a register with the branch to it still pending -- see SafepointSignalHandler's own comment
+  // (fex_guest_engine.cpp) for why that's unsafe specifically during a JIT buffer reuse, and why
+  // redirecting back to DispatcherLoopTop from anywhere in this range is always safe (the
+  // routine has no observable side effect before its final branch, so redoing it from scratch
+  // loses nothing).
+  uint64_t DispatcherBlockLookupRangeEnd {};
   uint64_t DispatcherLoopTopFillSRA {};
   uint64_t DispatcherLoopTopEnterEC {};
   uint64_t DispatcherLoopTopEnterECFillSRA {};
   uint64_t ExitFunctionLinker {};
+  // Raw address of a process-wide std::atomic<uint64_t> (owned by GuestEngine, not FEXCore --
+  // see ExitFunctionLinkValidatedGeneration's own comment, above) that counts completed JIT
+  // buffer reuse-in-place cycles. Not a JIT code address -- this is plain data, so unlike every
+  // other pointer in this struct it is NOT translated through GetExecutableAddress() when
+  // populated (InitThreadPointers, Dispatcher.cpp). ExitFunctionLinker (Dispatcher.cpp) loads
+  // this address, dereferences it fresh right before its final branch, and compares against
+  // ExitFunctionLinkValidatedGeneration.
+  uint64_t ReuseGenerationCounterAddress {};
+  // End of ExitFunctionLinker's own routine -- a second window with the same lock-free
+  // read-cached-target-then-branch hazard as DispatcherBlockLookupRangeEnd above, see that
+  // field's comment. [ExitFunctionLinker, ExitFunctionLinkerRangeEnd) is the range
+  // SafepointSignalHandler checks and redirects out of, same as the block-lookup range.
+  uint64_t ExitFunctionLinkerRangeEnd {};
   uint64_t ThreadStopHandlerSpillSRA {};
   uint64_t ThreadPauseHandlerSpillSRA {};
   uint64_t GuestSignal_SIGILL {};
@@ -427,6 +450,23 @@ struct CpuStateFrame {
 
   InternalThreadState* Thread;
 
+  // Set by ExitFunctionLink (JIT.cpp) right before every return, to the JIT-buffer-reuse
+  // generation counter's value at the moment it validated the HostCode it's returning.
+  // ExitFunctionLinker (Dispatcher.cpp) re-reads the live counter right before branching to
+  // that HostCode and compares against this -- if a reuse happened in the few instructions
+  // between the C++ call returning and that branch, this value is now stale relative to the
+  // live counter, and ExitFunctionLinker redirects to DispatcherLoopTop instead of trusting a
+  // host address that may no longer be valid. See ExitFunctionLink's own comment (JIT.cpp) and
+  // JITPointers::ReuseGenerationCounterAddress (below) for the full story -- this exists because
+  // that specific window has no lock of any kind (the C++ function's own CodeInvalidationMutex
+  // hold is already released by the time it returns), and the safepoint signal that protects
+  // every other such window can't help here either: signal delivery has real, nonzero latency,
+  // and a thread whose own trip through this narrow window happens to finish faster than that
+  // latency can execute the stale branch before ever being paused. Confirmed on-device as the
+  // actual remaining source of the "instruction-fetch fault, not inside any currently-live JIT
+  // allocation" crash even with the signal-based redirect already in place for this same range.
+  uint64_t ExitFunctionLinkValidatedGeneration {};
+
 #ifdef ARCHITECTURE_arm64ec
   // Set by the kernel on ARM64EC whenever the JIT should cooperatively suspend running guest code.
   uint32_t SuspendDoorbell {};
